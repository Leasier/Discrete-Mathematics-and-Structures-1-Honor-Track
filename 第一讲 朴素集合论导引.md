#### 定义 1.1.1 (集合的势之间的关系)

设 $S, T$ 为集合：

- (1) 称 $|S| \leq |T|$，若存在 **单射** $S \to T$，也记作 $S \preccurlyeq T$。
- (2) 称 $|S| = |T|$，若存在 **双射** $S \to T$，也记作 $S \sim T$。
- (3) 称 $|S| < |T|$，若 $|S| \leq |T|$ 但 $|S| = |T|$ 不成立，也记作 $S \prec T$。

易见 $\sim$ 为 **等价** 关系，且商掉 $\sim$ 后 $\preccurlyeq$ 为 **预序** 关系。进一步地欲说明其为 **偏序** 关系，这无非是说下面的定理成立。

#### 定理 1.1.2 (Bernstein-Cantor)

> 设 $S, T$ 为集合，若 $S \preccurlyeq T, T \preccurlyeq S$，则 $S \sim T$。

_Proof._ 设有单射 $f : S \to T, g : T \to S$。

> _Intuition._ 考虑一张由 $S, T$ 二部构成的有向图，有边 $x \to f(x), g(y) \leftarrow y$，则图中每个点至多一条入边、恰有一条出边。现考虑将其视为无向图时的联通块，可以想象（若着眼“每个有限的部分”）其为下面四者之一：
> > (i) 从 $x \in S \backslash \text{im}(g)$ 出发无限延伸。\
> > (ii) 从 $y \in T \backslash \text{im}(f)$ 出发无限延伸。\
> > (iii) 偶环。\
> > (iv) 向两端无限延伸。
>
> 对于 (i)(iii)(iv) 三种情形，令 $x \mapsto f(x)$；对于 (ii)，令 $g(y) \mapsto y$。

令 $A_0 = S \backslash \text{im}(g)$，迭代

$$
A_{n + 1} = g(f(A_n))
$$

最后令

$$
A = \bigcup_{n \in \mathbb{N}} A_n
$$

由此构造映射

$$
\begin{aligned}
h : S &\to T \\
x &\mapsto f(x), &\quad x \in A \\
x &\mapsto g^{-1}(x), &\quad x \not\in A
\end{aligned}
$$

容易验证 $h$ 确为双射，故 $S \sim T$。

#### 定理 1.1.3 (Cantor 定理)

> 设 $S$ 为集合，则 $S \prec \mathcal{P}(S)$。

_Proof._ 构造映射

$$
\begin{aligned}
i : S &\to \mathcal{P}(S) \\
x &\mapsto \{x\}
\end{aligned}
$$

显见 $i$ 为单射，故 $S \preccurlyeq \mathcal{P}(S)$。假设 $S \sim \mathcal{P}(S)$，即存在双射 $f : S \to \mathcal{P}(S)$，令

$$
R = \{x \in S \mid x \not\in f(x)\} \in \mathcal{P}(S)
$$

由满性可知 $\exists x_0 \in S, \text{s.t. } f(x_0) = R$。若 $x_0 \in R$ 则 $x_0 \not\in f(x_0) = R$，若 $x_0 \not\in R = f(x_0)$ 则 $x_0 \in R$，矛盾！

故不存在这样的双射 $f$ 即 $S \sim \mathcal{P}(S)$ 不成立，故 $S \prec \mathcal{P}(S)$。

#### 定义 1.1.4 (Dedekind 有限)

称集合 $S$ Dedekind 有限，若 $\forall T \subsetneq S, T \prec S$。
