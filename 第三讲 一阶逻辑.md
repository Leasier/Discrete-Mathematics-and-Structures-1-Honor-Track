### 3.1 基本概念

#### 定义 3.1.1 (字母表)

定义下列符号构成一阶逻辑的字母表 $\Sigma$：

- (1) 可数个变量符号：$x_0, x_1, \cdots$，构成集合 $\text{Var}$。
- (2) 可数个函数符号：$f_0, f_1, \cdots$，并配有确定的元数 $k \in \mathbb{N}$。
- (3) 可数个谓词符号：$P_0, P_1, \cdots$，同样配有确定的元数 $k \in \mathbb{N}$；以及二元的相等符号 $=$。
- (4) 常量符号：$\bar{c}_i$，也可视作 $0$ 元函数。
- (5) 连接词：$\neg, \land, \lor, \to$。
- (6) 量词：$\forall, \exists$。
- (7) 括号：$(, )$。

#### 定义 3.1.2 (项)

项集合 $\text{TERM} \subset \Sigma^*$ 归纳定义如下：

- (1) 所有变量符号和常量符号都在 $\text{TERM}$ 中。
- (2) 若 $f$ 为 $n$ 元函数符号，$t_1, \cdots, t_n \in \text{TERM}$，则 $f(t_1, \cdots, t_n) \in \text{TERM}$。

#### 定义 3.1.3 (命题公式)

命题公式集合 $\text{FOR} \subset \Sigma^*$ 归纳定义如下：

- (1) 若 $P$ 为 $n$ 元谓词符号，$t_1, \cdots, t_n \in \text{TERM}$，则 $P(t_1, \cdots, t_n) \in \text{FOR}$；特别地，$t_1, t_2 \in \text{TERM}$ 时 $(t_1 = t_2) \in \text{FOR}$。
- (2) $\bot \in \text{FOR}$。
- (3) 若 $\varphi, \psi \in \text{FOR}$，则 $\neg \varphi, (\varphi \land \psi), (\varphi \lor \psi), (\varphi \to \psi) \in \text{FOR}$。
- (4) 若 $\varphi \in \text{FOR}$、$x$ 为变量，则 $\forall x \ \varphi, \exists x \ \varphi \in \text{FOR}$。

#### 定义 3.1.4 (自由变量, 闭式)

记 $\text{FV}(t)$ 为 $t$ 中自由出现的变量构成的集合，归纳定义如下：

- (1) $\text{FV}(x) = \{x\}$。
- (2) $\text{FV}(\bar{c}) = \varnothing$。
- (3) $\text{FV}(f(t_1, \cdots, t_n)) = \displaystyle\bigcup_{i = 1}^n \text{FV}(t_i)$。
- (4) $\text{FV}(P(t_1, \cdots, t_n)) = \displaystyle\bigcup_{i=1}^n \text{FV}(t_i)$，$\text{FV}(t_1 = t_2) = \text{FV}(t_1) \cup \text{FV}(t_2)$。
- (5) $\text{FV}(\neg \varphi) = \text{FV}(\varphi)$。
- (6) $\text{FV}(\varphi \square \psi) = \text{FV}(\varphi) \cup \text{FV}(\psi)$，其中 $\square \in \{\land, \lor, \to\}$；
- (7) $\text{FV}(\forall x \ \varphi) = \text{FV}(\exists x \ \varphi) = \text{FV}(\varphi) \backslash \{x\}$。

无自由变量的公式称为闭式。

#### 定义 3.1.5 (结构, 赋值, 语义蕴涵式)

结构指四元组

$$
\mathfrak{A} = \langle \Omega; \{P_i\}; \{f_i\}; \{\bar{c}_i\} \rangle
$$

其中：

- (1) $\Omega \neq \varnothing$ 为论域；
- (2) 每个 $n$ 元谓词符号 $P_i$ 对应其解释 $P_i \subset \Omega^n$；
- (3) 每个 $n$ 元函数符号 $f_i$ 对应其解释 $f_i : \Omega^n \to \Omega$；
- (4) 每个常量 $\bar{c}_i$ 对应元素 $c_i \in \Omega$。

将 $\forall v \in \Omega, \bar{v}$ 扩充加入常量集后，设 $\text{TERM}'$ 表示不含变量符号的项的集合，$\text{FOR}'$ 表示所有命题公式的集合。

归纳定义项的赋值 $V : \text{TERM}' \to \Omega$：

- (1) $\llbracket \bar{c} \rrbracket_V = c$，$\llbracket \bar{v} \rrbracket_V = v$。
- (2) $\llbracket f(t_1, \cdots, t_n) \rrbracket_V = f(\llbracket t_1 \rrbracket_V, \cdots, \llbracket t_n \rrbracket_V)$。

再定义命题公式的赋值 $V : \text{FOR}' \to \{0, 1\}$：

- (1) $\llbracket P(t_1, \cdots, t_n) \rrbracket_V = [(\llbracket t_1 \rrbracket_V, \cdots, \llbracket t_n \rrbracket_V) \in P]$，$\llbracket t_1 = t_2 \rrbracket_V = [\llbracket t_1 \rrbracket_V = \llbracket t_2 \rrbracket_V]$。
- (2) $\bot, \neg, \land, \lor, \to$ 的语义定义同 **定义 2.1.4**。
- (3) $\llbracket \forall x \ \varphi \rrbracket_V = \displaystyle\min_{v \in \Omega} \llbracket \varphi [x / \bar{v}] \rrbracket_V$。
- (4) $\llbracket \exists x \ \varphi \rrbracket_V = \displaystyle\max_{v \in \Omega} \llbracket \varphi [x / \bar{v}] \rrbracket_V$。

其中 $\varphi [x / \bar{v}]$ 表示将 $\varphi$ 中所有自由出现的 $x$ 替换为 $\bar{v}$ 后所得公式。

最后定义原本命题公式的赋值 $V : \text{FOR} \hookrightarrow \text{FOR}' \to \{0, 1\}$。

$V \models \Gamma, \Gamma \models \varphi$ 的定义与 **定义 2.1.5** 一致。

### 3.2 自然演绎法

本节在 **2.2** 的基础上，补充量词法则和相等公理。

#### 规则 3.2.1 (量词法则)

我们将 $\exists x \ \varphi$ 定义 $\neg \forall x \ \neg \varphi$，并给出 $\forall$ 的法则：

> (1) 全称的引入法则：
> $$
> \frac{\varphi}{\forall x \ \varphi} \ \forall \ I, \quad x \not\in \text{FV}(\varphi)
> $$
> (2) 全称的消去法则：
> $$
> \frac{\forall x \ \varphi}{\varphi [x/t]} \ \forall \ E, \quad t \in \text{TERM}, t \text{ 对 } x \text{ 在 } \varphi \text{ 中自由}
> $$

其中项 $t$ 对 $x$ 在 $\varphi$ 中自由 ($t$ is free for $x$ in $\varphi$) 是指：

- 若把 $\varphi$ 中 $x$ 的 **自由出现** 替换为 $t$，$t$ 中新出现的变量均不被 $\varphi$ 原有的量词所捕获。

听上去或许有些拗口，直观地说命题 $\text{Free}(\varphi, x, t)$ 归纳定义如下：

- (1) $\text{Free}(P(t_1, \cdots, t_n), x, t), \text{Free}(t_1 = t_2, x, t)$ 成立。
- (2) $\text{Free}(\bot, x, t)$ 成立，$\text{Free}(\neg \varphi, x, t)$ 成立当且仅当 $\text{Free}(\varphi, x, t)$ 成立，$\text{Free}(\varphi_1 \square \varphi_2, x, t)$ 成立当且仅当 $\text{Free}(\varphi_1, x, t) \land \text{Free}(\varphi_2, x, t)$ 成立。
- (3) $\text{Free}(\forall x \ \varphi, x, t)$ 成立，$\text{Free}(\forall y \ \varphi, x, t) \ (y \neq x)$ 成立当且仅当 $x \in \text{FV}(\varphi) \land y \in \text{FV}(t)$ 不成立。

例如：

- $f(x)$ 对 $x$ 在 $\forall x \ (x = y)$ 中自由，因为没有需要进行的替换。
- $f(x, y)$ 对 $x$ 在 $\forall y \ P(x, y)$ 中不自由。

#### 规则 3.2.2 (相等公理)

> (1) 自反性：$\forall x \ (x = x)$。\
> (2) 对称性：$\forall x \ \forall y \ ((x = y) \to (y = x))$。\
> (3) 传递性：$\forall x \ \forall y \ \forall z \ (((x = y) \land (y = z)) \to (x = z))$。\
> (4) 项的等价：$\forall x_1 \ \cdots \ \forall x_n \ \forall y_1 \ \cdots \ \forall y_n \ \left( \displaystyle\bigwedge_{i = 1}^n (x_i = y_i) \to (t(x_1, \cdots, x_n) = t(y_1, \cdots, y_n)) \right)$。\
> (5) 命题公式的等价：$\forall x_1 \ \cdots \ \forall x_n \ \forall y_1 \ \cdots \ \forall y_n \ \left( \displaystyle\bigwedge_{i = 1}^n (x_i = y_i) \to (\varphi(x_1, \cdots, x_n) \leftrightarrow \varphi(y_1, \cdots, y_n)) \right)$，此处 $P \leftrightarrow Q$ 为 $(P \to Q) \land (Q \to P)$ 之缩写。

#### 例子 3.2.3

在只有 $=$ 这一个谓词符号（不含函数、其他谓词和常量）的语言中：

- (1) $\exists x_1 \exists x_2 \ \neg (x_1 = x_2)$ 表达论域中 **至少有两个** 元素。
- (2) $\forall x_1 \forall x_2 \ (x_1 = x_2)$ 表达论域中 **至多有一个** 元素。
