### 3.1 基本概念

#### 定义 3.1.1 (字母表)

定义下列符号构成一阶逻辑的字母表 $\Sigma$：

- (1) 可数个变量符号：$x_0, x_1, \cdots$，构成集合 $\text{Var}$。
- (2) 可数个函数符号：$f_0, f_1, \cdots$，并配有确定的元数 $k \in \mathbb{N}$。
- (3) 可数个谓词符号：$P_0, P_1, \cdots$，同样配有确定的元数 $k \in \mathbb{N}$；以及零元的矛盾符号 $\bot$ 和二元的相等符号 $=$。
- (4) 常量符号：$\bar{c}_i$，也可视作零元函数。
- (5) 连接词：$\neg, \land, \lor, \to$。
- (6) 量词：$\forall$。
- (7) 括号：$(, )$。

#### 定义 3.1.2 (项)

项集合 $\text{TERM} \subset \Sigma^*$ 归纳定义如下：

- (1) 所有变量符号和常量符号都在 $\text{TERM}$ 中。
- (2) 若 $f$ 为 $n$ 元函数符号，$t_1, \cdots, t_n \in \text{TERM}$，则 $f(t_1, \cdots, t_n) \in \text{TERM}$。

#### 定义 3.1.3 (谓词公式)

谓词公式集合 $\text{FOR} \subset \Sigma^*$ 归纳定义如下：

- (1) 若 $P$ 为 $n$ 元谓词符号，$t_1, \cdots, t_n \in \text{TERM}$，则 $P(t_1, \cdots, t_n) \in \text{FOR}$；特别地，$t_1, t_2 \in \text{TERM}$ 时 $(t_1 = t_2) \in \text{FOR}$。
- (2) $\bot \in \text{FOR}$。
- (3) 若 $\varphi, \psi \in \text{FOR}$，则 $\neg \varphi, (\varphi \land \psi), (\varphi \lor \psi), (\varphi \to \psi) \in \text{FOR}$。
- (4) 若 $\varphi \in \text{FOR}$、$x$ 为变量，则 $\forall x \ \varphi \in \text{FOR}$。

> _Rmk._ 我们将 $\exists x \ \varphi$ 定义为 $\neg \forall x \ \neg \varphi$ 之缩写，因此不认为 $\exists$ 是字母表中的原语。

#### 定义 3.1.4 (自由变量, 闭式, 闭项)

记 $\text{FV}(t)$ 为 $t$ 中自由出现的变量构成的集合，归纳定义如下：

- (1) $\text{FV}(x) = \{x\}$。
- (2) $\text{FV}(\bar{c}) = \varnothing$。
- (3) $\text{FV}(f(t_1, \cdots, t_n)) = \displaystyle\bigcup_{i = 1}^n \text{FV}(t_i)$。
- (4) $\text{FV}(P(t_1, \cdots, t_n)) = \displaystyle\bigcup_{i = 1}^n \text{FV}(t_i)$，$\text{FV}(\bot) = \varnothing$，$\text{FV}(t_1 = t_2) = \text{FV}(t_1) \cup \text{FV}(t_2)$。
- (5) $\text{FV}(\neg \varphi) = \text{FV}(\varphi)$。
- (6) $\text{FV}(\varphi \square \psi) = \text{FV}(\varphi) \cup \text{FV}(\psi)$，其中 $\square \in \{\land, \lor, \to\}$。
- (7) $\text{FV}(\forall x \ \varphi) = \text{FV}(\varphi) \backslash \{x\}$。

无自由变量的谓词公式称为闭式，也称句子，记其构成集合 $\text{CLOSE}$；无自由变量的项（即只含常量和函数符号）称为闭项。

对于一族命题 $\Gamma$，令 $\text{FV}(\Gamma) = \displaystyle\bigcup_{\varphi \in \Gamma} \text{FV}(\varphi)$。

#### 定义 3.1.5 (替换)

设 $x$ 为变量，$s \in \text{TERM}$，$t \in \text{TERM} \cup \text{FOR}$，记 $t [s / x] \in \text{TERM} \cup \text{FOR}$ 为将 $t$ 中所有自由出现的 $x$ 替换为 $s$ 所得的项或谓词公式，归纳定义如下：

- (1) $\bar{c} [s / x] = \bar{c}$。
- (2) $x [s / x] = s, y [s / x] = y \ (y \neq x)$。
- (3) $P(t_1, \cdots, t_n) [s / x] = P(t_1 [s / x], \cdots, t_n [s / x])$，$\bot [s / x] = \bot$，$(t_1 = t_2) [s / x] = (t_1 [s / x] = t_2 [s / x])$。
- (4) $f(t_1, \cdots, t_n) [s / x] = f(t_1 [s / x], \cdots, t_n [s / x])$。
- (5) $\neg \varphi [s / x] = \neg (\varphi [s / x])$。
- (6) $(\varphi \square \psi) [s / x] = (\varphi [s / x] \square \psi [s / x])$，其中 $\square \in \{\land, \lor, \to\}$。
- (7) $\forall x \ \varphi [s / x] = \forall x \ \varphi, \forall y \ \varphi [s / x] = \forall y \ (\varphi [s / x]) \ (y \neq x)$。

有时我们也可能会进行若干 **同时** 的替换，如在 $f(x_1, x_2)$ 中同时替换 $x_1 \mapsto x_2, x_2 \mapsto x_1$，应当得到 $f(x_2, x_1)$，这与分步替换的结果并不一致。

#### 定义 3.1.6 (结构, 赋值, 满足关系, 语义蕴涵式)

结构指四元组

$$
\mathcal{M} = \langle \Omega; \{P_i\}; \{f_i\}; \{\bar{c}_i\} \rangle
$$

其中：

- (1) $\Omega \neq \varnothing$ 为论域；
- (2) 每个 $n$ 元谓词符号 $P_i$ 对应其解释 $P_i \subset \Omega^n$；
- (3) 每个 $n$ 元函数符号 $f_i$ 对应其解释 $f_i : \Omega^n \to \Omega$；
- (4) 每个常量 $\bar{c}_i$ 对应元素 $c_i \in \Omega$。

将 $\forall v \in \Omega, \bar{v}$ 扩充加入常量集后，设 $\text{TERM}'$ 表示项的集合，$\text{TERM}'_{\times}$ 表示不含变量符号的项的集合，$\text{CLOSE}'$ 表示所有 **闭式** 的集合。

归纳定义项的赋值 $\llbracket \cdot \rrbracket_{\mathcal{M}} : \text{TERM}'_{\times} \to \Omega$：

- (1) $\llbracket \bar{c} \rrbracket_{\mathcal{M}} = c$，$\llbracket \bar{v} \rrbracket_{\mathcal{M}} = v$。
- (2) $\llbracket f(t_1, \cdots, t_n) \rrbracket_{\mathcal{M}} = f(\llbracket t_1 \rrbracket_{\mathcal{M}}, \cdots, \llbracket t_n \rrbracket_{\mathcal{M}})$。

再定义闭式的赋值 $\llbracket \cdot \rrbracket_{\mathcal{M}} : \text{CLOSE}' \to \{0, 1\}$：

- (1) $\llbracket P(t_1, \cdots, t_n) \rrbracket_{\mathcal{M}} = [(\llbracket t_1 \rrbracket_{\mathcal{M}}, \cdots, \llbracket t_n \rrbracket_{\mathcal{M}}) \in P]$，$\llbracket t_1 = t_2 \rrbracket_{\mathcal{M}} = [\llbracket t_1 \rrbracket_{\mathcal{M}} = \llbracket t_2 \rrbracket_{\mathcal{M}}]$。
- (2) $\bot, \neg, \land, \lor, \to$ 的语义定义同 **定义 2.1.4**。
- (3) $\llbracket \forall x \ \varphi \rrbracket_{\mathcal{M}} = \displaystyle\min_{v \in \Omega} \llbracket \varphi [\bar{v} / x] \rrbracket_{\mathcal{M}}$。

其中 $\varphi [\bar{v} / x]$ 表示将 $\varphi$ 中所有 **自由出现** 的 $x$ 替换为 $\bar{v}$ 后所得公式。

最后将其限制为原本闭式的赋值 $\llbracket \cdot \rrbracket_{\mathcal{M}} : \text{CLOSE} \hookrightarrow \text{CLOSE}' \to \{0, 1\}$。

定义满足关系：

- (1) 设 $\varphi \in \text{CLOSE}$，定义 $\mathcal{M} \models \varphi$ 当且仅当 $\llbracket \varphi \rrbracket_{\mathcal{M}} = 1$。
- (2) 设 $\varphi \in \text{FOR}$ 有自由变量 $x_1, \cdots, x_n$，定义 $\mathcal{M} \models \varphi$ 当且仅当 $\mathcal{M} \models \forall x_1 \ \cdots \ \forall x_n \ \varphi$。

对于一族命题 $\Gamma$，定义语义蕴涵：

- (1) 定义 $\mathcal{M} \models \Gamma$ 当且仅当 $\forall \varphi \in \Gamma, \mathcal{M} \models \varphi$。
- (2) 定义 $\models \varphi$ 当且仅当 $\forall \mathcal{M}, \mathcal{M} \models \varphi$。
- (3) 设 $\Gamma$ 和 $\varphi \in \text{FOR}$ 有自由变量 $(x_i)_{i \in I}$，定义 $\Gamma \models \varphi$ 当且仅当 $\forall \mathcal{M}, \forall (v_i)_{i \in I} \in \Omega^I, (\mathcal{M} \models \Gamma [\bar{v}_i / x_i]_{i \in I} \Rightarrow \mathcal{M} \models \varphi [\bar{v}_i / x_i]_{i \in I})$。

> _Rmk._ 当 $\varphi$ 非闭式，不应当 $\Gamma \models \varphi$ 定义为 $\forall \mathcal{M}, (\mathcal{M} \models \Gamma \Rightarrow \mathcal{M} \models \varphi)$，例如 $\Gamma = \{P(x)\}, \varphi = P(y)$ 的情形。

### 3.2 自然演绎法

本节在 **2.2** 的基础上，补充量词法则和相等公理。

#### 规则 3.2.1 (量词法则)

下面给出 $\forall$ 的法则：

> (1) 全称的引入法则：
> $$
> \frac{\varphi}{\forall x \ \varphi} \ \forall I, \quad \text{对于推导所依赖的假设 } \psi, x \not\in \text{FV}(\psi)
> $$
> (2) 全称的消去法则：
> $$
> \frac{\forall x \ \varphi}{\varphi [t / x]} \ \forall E, \quad t \text{ 对 } x \text{ 在 } \varphi \text{ 中自由}
> $$

其中项 $t$ 对 $x$ 在 $\varphi$ 中自由 ($t$ is free for $x$ in $\varphi$) 是指：

- 若把 $\varphi$ 中 $x$ 的 **自由出现** 替换为 $t$，$t$ 中新出现的变量均不被 $\varphi$ 原有的量词所捕获。

听上去或许有些拗口，具体地说，命题 $\text{Free}(\varphi, x, t)$ 归纳定义如下：

- (1) $\text{Free}(P(t_1, \cdots, t_n), x, t), \text{Free}(t_1 = t_2, x, t)$ 成立。
- (2) $\text{Free}(\bot, x, t)$ 成立，$\text{Free}(\neg \varphi, x, t)$ 成立当且仅当 $\text{Free}(\varphi, x, t)$ 成立，$\text{Free}(\varphi_1 \square \varphi_2, x, t)$ 成立当且仅当 $\text{Free}(\varphi_1, x, t) \land \text{Free}(\varphi_2, x, t)$ 成立。
- (3) $\text{Free}(\forall x \ \varphi, x, t)$ 成立，$\text{Free}(\forall y \ \varphi, x, t) \ (y \neq x)$ 成立当且仅当 $x \not\in \text{FV}(\varphi)$，或者 $y \not\in \text{FV}(t)$ 且 $\text{Free}(\varphi, x, t)$ 成立。

例如：

- $f(x)$ 对 $x$ 在 $\forall x \ (x = y)$ 中自由，因为没有需要进行的替换。
- $f(x, y)$ 对 $x$ 在 $\forall y \ P(x, y)$ 中不自由。

用语法蕴含的方式来写，上面的法则就是：

$$
\begin{aligned}
(\forall I) &\quad \Gamma \vdash \varphi, \quad x \not\in \text{FV}(\Gamma)\Rightarrow \Gamma \vdash \forall x \ \varphi \\
(\forall E) &\quad \Gamma \vdash \forall x \ \varphi, \quad \text{Free}(\varphi, x, t) \Rightarrow \Gamma \vdash \varphi [t / x]
\end{aligned}
$$

#### 例子 3.2.2

设 $t$ 对 $x$ 在 $\varphi$ 中自由，下面在自然演绎框架中证明 $\varphi [t / x] \vdash \exists x \ \varphi$。

$$
\frac{\dfrac{\varphi [t / x] \quad \dfrac{[\forall x \ \neg \varphi]}{\neg \varphi [t / x]} \ \forall E}{\bot} \to E}{\neg \forall x \ \neg \varphi} \to I
$$

#### 例子 3.2.3

下面在自然演绎框架中证明 $\exists x \ \forall y \ \varphi(x, y) \vdash \forall y \ \exists x \ \varphi(x, y)$。

$$
\frac{\dfrac{\dfrac{\dfrac{\dfrac{\dfrac{\dfrac{[\forall y \ \varphi(x, y)]_2}{\varphi(x, y)} \ \forall E \quad \dfrac{[\forall x \ \neg \varphi(x, y)]_1}{\neg \varphi(x, y)} \ \forall E}{\bot} \to E}{\neg \forall y \ \varphi(x, y)} \to I_2}{\forall x \ \neg \forall y \ \varphi(x, y)} \ \forall I \quad \neg \forall x \ \neg \forall y \ \varphi(x, y)}{\bot} \to E}{\neg \forall x \ \neg \varphi(x, y)} \ \to I_1}{\forall y \ \exists x \ \varphi(x, y)} \ \forall I
$$

#### 规则 3.2.4 (相等公理)

> _Rmk._ 下面的 $t, \varphi$ 指的不是“预制”的函数和谓词符号，而是一般的项和谓词公式，并有 $\text{FV}(t), \text{FV}(\varphi) \subset \{x_1, \cdots, x_n\}$，同时 $t(y_1, \cdots, y_n)$ 指的是 $t [y_i / x_i]_{i = 1}^n$。

用公理模式写作：

> (1) 自反性：$\forall x \ (x = x)$。\
> (2) 对称性：$\forall x \ \forall y \ ((x = y) \to (y = x))$。\
> (3) 传递性：$\forall x \ \forall y \ \forall z \ (((x = y) \land (y = z)) \to (x = z))$。\
> (4) 项的等价：$\forall x_1 \ \cdots \ \forall x_n \ \forall y_1 \ \cdots \ \forall y_n \ \left( \displaystyle\bigwedge_{i = 1}^n (x_i = y_i) \to (t(x_1, \cdots, x_n) = t(y_1, \cdots, y_n)) \right)$。\
> (5) 谓词公式的等价：$\forall x_1 \ \cdots \ \forall x_n \ \forall y_1 \ \cdots \ \forall y_n \ \left( \displaystyle\bigwedge_{i = 1}^n (x_i = y_i) \to (\varphi(x_1, \cdots, x_n) \to \varphi(y_1, \cdots, y_n)) \right)$。\
> _Rmk._ (5) 可以结合对称性进一步推出 $\varphi(x_1, \cdots, x_n) \leftrightarrow \varphi(y_1, \cdots, y_n)$，其中 $P \leftrightarrow Q$ 为 $(P \to Q) \land (Q \to P)$ 之缩写。

用自然演绎规则写作：

> (1) 自反性：
> $$
> \frac{}{t = t} \ \text{RE}_1
> $$
> (2) 对称性：
> $$
> \frac{t_1 = t_2}{t_2 = t_1} \ \text{RE}_2
> $$
> (3) 传递性：
> $$
> \frac{t_1 = t_2 \quad t_2 = t_3}{t_1 = t_3} \ \text{RE}_3
> $$
> (4) 项的等价：若 $t_i, t'_i$ 对 $x_i$ 在 $t$ 中自由，则
> $$
> \dfrac{t_1 = t'_1 \quad \cdots \quad t_n = t'_n}{t(t_1, \cdots, t_n) = t(t'_1, \cdots, t'_n)} \ \text{RE}_4
> $$
> (5) 谓词公式的等价：若 $t_i, t'_i$ 对 $x_i$ 在 $\varphi$ 中自由，则
> $$
> \dfrac{t_1 = t'_1 \quad \cdots \quad t_n = t'_n \quad \varphi(t_1, \cdots, t_n)}{\varphi(t'_1, \cdots, t'_n)} \ \text{RE}_4
> $$

易见将这两者加入一阶逻辑是等价的。

#### 例子 3.2.5

在只有 $=$ 这一个谓词符号（不含函数、其他谓词和常量）的语言中：

- (1) $\exists x_1 \exists x_2 \ \neg (x_1 = x_2)$ 表达论域中 **至少有两个** 元素。
- (2) $\forall x_1 \forall x_2 \ (x_1 = x_2)$ 表达论域中 **至多有一个** 元素。
