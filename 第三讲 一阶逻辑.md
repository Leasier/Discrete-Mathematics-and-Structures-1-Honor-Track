### 3.1 基本概念

#### 定义 3.1.1 (字母表, 语言)

定义下列符号构成一阶逻辑的字母表 $\Sigma$：

- (1) 可数个变量符号：$x_0, x_1, \cdots$，构成集合 $\text{Var}$。
- (2) 可数个函数符号：$f_0, f_1, \cdots$，并分别配有确定的元数 $k \in \mathbb{N}$。
- (3) 可数个谓词符号：$P_0, P_1, \cdots$，同样分别配有确定的元数 $k \in \mathbb{N}$；以及逻辑符号 $\bot$ 和二元的相等符号 $=$。
- (4) 常量符号：$\bar{c}_i$，也可视作零元函数。
- (5) 连接词：$\neg, \land, \lor, \to$。
- (6) 量词：$\forall$。
- (7) 括号：$(, )$。

由 $\Sigma$ 中的非逻辑符号构成的集合称为一个语言 $L$。若 $L \subset L'$，则称语言 $L'$ 是 $L$ 的扩张。

#### 定义 3.1.2 (项)

项集合 $\text{TERM} \subset \Sigma^*$ 归纳定义如下：

- (1) 所有变量符号和常量符号都在 $\text{TERM}$ 中。
- (2) 若 $f$ 为 $n$ 元函数符号，$t_1, \cdots, t_n \in \text{TERM}$，则 $f(t_1, \cdots, t_n) \in \text{TERM}$。

#### 定义 3.1.3 (谓词公式)

谓词公式集合 $\text{FOR} \subset \Sigma^*$ 归纳定义如下：

- (1) 若 $P$ 为 $n$ 元谓词符号，$t_1, \cdots, t_n \in \text{TERM}$，则 $P(t_1, \cdots, t_n) \in \text{FOR}$；特别地，$t_1, t_2 \in \text{TERM}$ 时 $(t_1 = t_2) \in \text{FOR}$。
- (2) $\bot \in \text{FOR}$。
- (3) 若 $\varphi, \psi \in \text{FOR}$，则 $\neg \varphi, (\varphi \land \psi), (\varphi \lor \psi), (\varphi \to \psi) \in \text{FOR}$。
- (4) 若 $\varphi \in \text{FOR}$、$x$ 为变量，则 $\forall x \ \varphi \in \text{FOR}$。

> _Rmk._ 我们将 $\exists x \ \varphi$ 定义为 $\neg \forall x \ \neg \varphi$ 之缩写，因此不认为 $\exists$ 是字母表中的原语。\
> 当然，对称地，我们也可以认为 $\exists$ 才是字母表中的原语：只需将 $\forall x \ \varphi$ 定义为 $\neg \exists x \ \neg \varphi$。

#### 定义 3.1.4 (自由变量, 闭式, 闭项)

记 $\text{FV}(t)$ 为 $t$ 中自由出现的变量构成的集合，归纳定义如下：

- (1) $\text{FV}(x) = \{x\}$。
- (2) $\text{FV}(\bar{c}) = \varnothing$。
- (3) $\text{FV}(f(t_1, \cdots, t_n)) = \displaystyle\bigcup_{i = 1}^n \text{FV}(t_i)$。
- (4) $\text{FV}(P(t_1, \cdots, t_n)) = \displaystyle\bigcup_{i = 1}^n \text{FV}(t_i)$，$\text{FV}(\bot) = \varnothing$，$\text{FV}(t_1 = t_2) = \text{FV}(t_1) \cup \text{FV}(t_2)$。
- (5) $\text{FV}(\neg \varphi) = \text{FV}(\varphi)$。
- (6) $\text{FV}(\varphi \square \psi) = \text{FV}(\varphi) \cup \text{FV}(\psi)$，其中 $\square \in \{\land, \lor, \to\}$。
- (7) $\text{FV}(\forall x \ \varphi) = \text{FV}(\varphi) \backslash \{x\}$。

对于一族公式 $\Gamma$，令 $\text{FV}(\Gamma) = \displaystyle\bigcup_{\varphi \in \Gamma} \text{FV}(\varphi)$。

无自由变量的谓词公式称为闭式，也称句子，记其构成集合 $\text{CLOSE}$；无自由变量的项（即只含常量和函数符号）称为闭项。

$L$ 中公式与闭式构成的集合分别记作 $\text{FOR}_L$ 与 $\text{CLOSE}_L$，而扩张 $L' \supset L$ 上的这两个集合也简记作 $\text{FOR}'$ 与 $\text{CLOSE}'$。

#### 定义 3.1.5 (替换)

设 $x$ 为变量，$s \in \text{TERM}$，$t \in \text{TERM} \cup \text{FOR}$，记 $t [s / x] \in \text{TERM} \cup \text{FOR}$ 为将 $t$ 中所有自由出现的 $x$ 替换为 $s$ 所得的项或谓词公式，归纳定义如下：

- (1) $\bar{c} [s / x] = \bar{c}$。
- (2) $x [s / x] = s, y [s / x] = y \ (y \neq x)$。
- (3) $P(t_1, \cdots, t_n) [s / x] = P(t_1 [s / x], \cdots, t_n [s / x])$，$\bot [s / x] = \bot$，$(t_1 = t_2) [s / x] = (t_1 [s / x] = t_2 [s / x])$。
- (4) $f(t_1, \cdots, t_n) [s / x] = f(t_1 [s / x], \cdots, t_n [s / x])$。
- (5) $\neg \varphi [s / x] = \neg (\varphi [s / x])$。
- (6) $(\varphi \square \psi) [s / x] = (\varphi [s / x] \square \psi [s / x])$，其中 $\square \in \{\land, \lor, \to\}$。
- (7) $\forall x \ \varphi [s / x] = \forall x \ \varphi, \forall y \ \varphi [s / x] = \forall y \ (\varphi [s / x]) \ (y \neq x)$。

有时我们也可能会进行若干 **同时** 的替换，如在 $f(x_1, x_2)$ 中同时替换 $x_1 \mapsto x_2, x_2 \mapsto x_1$，应当得到 $f(x_2, x_1)$，这与分步替换的结果并不一致。

#### 定义 3.1.6 (结构, 赋值, 语义蕴涵式)

结构指四元组

$$
\mathcal{M} = \langle \Omega; \{P_i\}; \{f_i\}; \{\bar{c}_i\} \rangle
$$

其中：

- (1) $\Omega \neq \varnothing$ 为论域；
- (2) 每个 $n$ 元谓词符号 $P_i$ 对应其解释 $P_i \subset \Omega^n$；
- (3) 每个 $n$ 元函数符号 $f_i$ 对应其解释 $f_i : \Omega^n \to \Omega$；
- (4) 每个常量 $\bar{c}_i$ 对应元素 $c_i \in \Omega$。

将 $\forall v \in \Omega, \bar{v}$ 扩充加入常量集后，设 $\text{TERM}'$ 表示项的集合，$\text{TERM}'_{\times}$ 表示不含变量符号的项的集合，$\text{CLOSE}'$ 表示所有 **闭式** 的集合。

归纳定义项的赋值 $\llbracket \cdot \rrbracket_{\mathcal{M}} : \text{TERM}'_{\times} \to \Omega$：

- (1) $\llbracket \bar{c} \rrbracket_{\mathcal{M}} = c$，$\llbracket \bar{v} \rrbracket_{\mathcal{M}} = v$。
- (2) $\llbracket f(t_1, \cdots, t_n) \rrbracket_{\mathcal{M}} = f(\llbracket t_1 \rrbracket_{\mathcal{M}}, \cdots, \llbracket t_n \rrbracket_{\mathcal{M}})$。

再定义闭式的赋值 $\llbracket \cdot \rrbracket_{\mathcal{M}} : \text{CLOSE}' \to \{0, 1\}$：

- (1) $\llbracket P(t_1, \cdots, t_n) \rrbracket_{\mathcal{M}} = [(\llbracket t_1 \rrbracket_{\mathcal{M}}, \cdots, \llbracket t_n \rrbracket_{\mathcal{M}}) \in P]$，$\llbracket t_1 = t_2 \rrbracket_{\mathcal{M}} = [\llbracket t_1 \rrbracket_{\mathcal{M}} = \llbracket t_2 \rrbracket_{\mathcal{M}}]$。
- (2) $\bot, \neg, \land, \lor, \to$ 的语义定义同 **定义 2.1.4**。
- (3) $\llbracket \forall x \ \varphi \rrbracket_{\mathcal{M}} = \displaystyle\min_{v \in \Omega} \llbracket \varphi [\bar{v} / x] \rrbracket_{\mathcal{M}}$。

其中 $\varphi [\bar{v} / x]$ 表示将 $\varphi$ 中所有 **自由出现** 的 $x$ 替换为 $\bar{v}$ 后所得公式。

最后将其限制在原本的闭式 $\text{CLOSE}$ 上，仍记作 $\llbracket \cdot \rrbracket_{\mathcal{M}} : \text{CLOSE} \to \{0, 1\}$。

定义满足关系：

- (1) 设 $\varphi \in \text{CLOSE}$，定义 $\mathcal{M} \models \varphi$ 当且仅当 $\llbracket \varphi \rrbracket_{\mathcal{M}} = 1$。
- (2) 设 $\varphi \in \text{FOR}$ 有自由变量 $x_1, \cdots, x_n$，定义 $\mathcal{M} \models \varphi$ 当且仅当 $\mathcal{M} \models \forall x_1 \ \cdots \ \forall x_n \ \varphi$。

进一步地，设 $\Gamma$ 代表一族公式（“上下文”）：

- (1) 定义 $\mathcal{M} \models \Gamma$ 当且仅当 $\forall \varphi \in \Gamma, \mathcal{M} \models \varphi$。
- (2) 定义 $\models \varphi$ 当且仅当 $\forall \mathcal{M}, \mathcal{M} \models \varphi$。
- (3) 设 $\Gamma$ 和 $\varphi \in \text{FOR}$ 的自由变量集合为 $\{x_i\}_{i \in I} = \text{FV}(\Gamma) \cup \text{FV}(\varphi)$，定义 $\Gamma \models \varphi$ 当且仅当 $\forall \mathcal{M}, \forall (v_i)_{i \in I} \in \Omega^I, (\mathcal{M} \models \Gamma [\bar{v}_i / x_i]_{i \in I} \Rightarrow \mathcal{M} \models \varphi [\bar{v}_i / x_i]_{i \in I})$。

> _Rmk._ 当 $\varphi$ 非闭式，不应当 $\Gamma \models \varphi$ 定义为 $\forall \mathcal{M}, (\mathcal{M} \models \Gamma \Rightarrow \mathcal{M} \models \varphi)$，例如 $\Gamma = \{P(x)\}, \varphi = P(y)$ 的情形。

### 3.2 自然演绎法

本节在 **2.2** 的基础上，补充量词法则和相等公理。

#### 规则 3.2.1 (量词法则)

下面给出 $\forall$ 的法则：

> (1) 全称的引入法则：
> $$
> \frac{\varphi}{\forall x \ \varphi} \ \forall I, \quad \text{对于推导所依赖的假设 } \psi, x \not\in \text{FV}(\psi)
> $$
> (2) 全称的消去法则：
> $$
> \frac{\forall x \ \varphi}{\varphi [t / x]} \ \forall E, \quad t \text{ 对 } x \text{ 在 } \varphi \text{ 中自由}
> $$

其中项 $t$ 对 $x$ 在 $\varphi$ 中自由 ($t$ is free for $x$ in $\varphi$) 是指：

- 若把 $\varphi$ 中 $x$ 的 **自由出现** 替换为 $t$，$t$ 中新出现的变量均不被 $\varphi$ 原有的量词所捕获。

具体地说，$\text{Free}(\varphi, x, t)$ 归纳定义如下：

- (1) $\text{Free}(P(t_1, \cdots, t_n), x, t), \text{Free}(t_1 = t_2, x, t)$ 成立。
- (2) $\text{Free}(\bot, x, t)$ 成立，$\text{Free}(\neg \varphi, x, t)$ 成立当且仅当 $\text{Free}(\varphi, x, t)$ 成立，$\text{Free}(\varphi_1 \square \varphi_2, x, t)$ 成立当且仅当 $\text{Free}(\varphi_1, x, t) \land \text{Free}(\varphi_2, x, t)$ 成立。
- (3) $\text{Free}(\forall x \ \varphi, x, t)$ 成立，$\text{Free}(\forall y \ \varphi, x, t) \ (y \neq x)$ 成立当且仅当 $x \not\in \text{FV}(\varphi)$，或者 $y \not\in \text{FV}(t)$ 且 $\text{Free}(\varphi, x, t)$ 成立。

例如：

- $f(x)$ 对 $x$ 在 $\forall x \ (x = y)$ 中自由，因为没有需要进行的替换。
- $f(x, y)$ 对 $x$ 在 $\forall y \ P(x, y)$ 中不自由。

上面的法则也可写为：

$$
\begin{aligned}
(\forall I) &\quad \Gamma \vdash \varphi, \quad x \not\in \text{FV}(\Gamma)\Rightarrow \Gamma \vdash \forall x \ \varphi \\
(\forall E) &\quad \Gamma \vdash \forall x \ \varphi, \quad \text{Free}(\varphi, x, t) \Rightarrow \Gamma \vdash \varphi [t / x]
\end{aligned}
$$

#### 规则 & 例子 3.2.2 ($\exists I$)

设 $t$ 对 $x$ 在 $\varphi$ 中自由，下面在自然演绎框架中证明 $\varphi [t / x] \vdash \exists x \ \varphi$。

$$
\frac{\dfrac{\varphi [t / x] \quad \dfrac{[\forall x \ \neg \varphi]}{\neg \varphi [t / x]} \ \forall E}{\bot} \to E}{\neg \forall x \ \neg \varphi} \to I
$$

这就是存在的引入法则 $\exists I$。

#### 规则 & 例子 3.2.3 ($\exists E$)

设 $x \not\in \text{FV}(\Gamma) \cup \text{FV}(\psi)$，下面在自然演绎框架中证明 $\Gamma, \varphi \vdash \psi \Rightarrow \Gamma, \exists x \ \varphi(x) \vdash \psi$：

$$
\frac{\dfrac{\dfrac{\dfrac{\dfrac{\dfrac{\dfrac{[\varphi]_2}{\vdots}}{\psi} \quad [\neg \psi]_1}{\bot} \to E}{\neg \varphi} \to I_2}{\forall x \ \neg \varphi} \ \forall I \quad \neg \forall x \ \neg \varphi}{\bot} \to E}{\psi} \ \text{RAA}_1
$$

这就是存在的消去法则 $\exists E$。更一般地，我们将下面这条加入推导规则：

$$
\frac{\exists x \ \varphi \quad \dfrac{\dfrac{[\varphi [y / x]]}{\vdots}}{\psi}}{\psi} \ \exists E, \quad \begin{cases}
y \not\in \text{FV}(\psi), \text{Free}(\varphi, x, y) \\
\text{对于推导所依赖的其他假设 } \theta, y \not\in \text{FV}(\theta)
\end{cases}
$$

#### 例子 3.2.4

下面在自然演绎框架中证明 $\exists x \ \forall y \ \varphi(x, y) \vdash \forall y \ \exists x \ \varphi(x, y)$。

$$
\frac{\dfrac{\dfrac{\dfrac{\dfrac{\dfrac{\dfrac{[\forall y \ \varphi(x, y)]_2}{\varphi(x, y)} \ \forall E \quad \dfrac{[\forall x \ \neg \varphi(x, y)]_1}{\neg \varphi(x, y)} \ \forall E}{\bot} \to E}{\neg \forall y \ \varphi(x, y)} \to I_2}{\forall x \ \neg \forall y \ \varphi(x, y)} \ \forall I \quad \neg \forall x \ \neg \forall y \ \varphi(x, y)}{\bot} \to E}{\neg \forall x \ \neg \varphi(x, y)} \ \to I_1}{\forall y \ \exists x \ \varphi(x, y)} \ \forall I
$$

#### 规则 3.2.5 (相等公理)

> _Rmk._ 下面的 $t, \varphi$ 指的不是“预制”的函数和谓词符号，而是一般的项和谓词公式，并有 $\text{FV}(t), \text{FV}(\varphi) \subset \{x_1, \cdots, x_n\}$，同时 $t(y_1, \cdots, y_n)$ 指的是 $t [y_i / x_i]_{i = 1}^n$。

用公理模式写作：

> (1) 自反性：$\forall x \ (x = x)$。\
> (2) 对称性：$\forall x \ \forall y \ ((x = y) \to (y = x))$。\
> (3) 传递性：$\forall x \ \forall y \ \forall z \ (((x = y) \land (y = z)) \to (x = z))$。\
> (4) 项的等价：$\forall x_1 \ \cdots \ \forall x_n \ \forall y_1 \ \cdots \ \forall y_n \ \left( \displaystyle\bigwedge_{i = 1}^n (x_i = y_i) \to (t(x_1, \cdots, x_n) = t(y_1, \cdots, y_n)) \right)$。\
> (5) 谓词公式的等价：$\forall x_1 \ \cdots \ \forall x_n \ \forall y_1 \ \cdots \ \forall y_n \ \left( \displaystyle\bigwedge_{i = 1}^n (x_i = y_i) \to (\varphi(x_1, \cdots, x_n) \to \varphi(y_1, \cdots, y_n)) \right)$。\
> _Rmk._ (5) 可以结合对称性进一步推出 $\varphi(x_1, \cdots, x_n) \leftrightarrow \varphi(y_1, \cdots, y_n)$，其中 $P \leftrightarrow Q$ 为 $(P \to Q) \land (Q \to P)$ 之缩写。

用自然演绎规则写作：

> (1) 自反性：
> $$
> \frac{}{t = t} \ \text{RE}_1
> $$
> (2) 对称性：
> $$
> \frac{t_1 = t_2}{t_2 = t_1} \ \text{RE}_2
> $$
> (3) 传递性：
> $$
> \frac{t_1 = t_2 \quad t_2 = t_3}{t_1 = t_3} \ \text{RE}_3
> $$
> (4) 项的等价：若 $t_i, t'_i$ 对 $x_i$ 在 $t$ 中自由，则
> $$
> \dfrac{t_1 = t'_1 \quad \cdots \quad t_n = t'_n}{t(t_1, \cdots, t_n) = t(t'_1, \cdots, t'_n)} \ \text{RE}_4
> $$
> (5) 谓词公式的等价：若 $t_i, t'_i$ 对 $x_i$ 在 $\varphi$ 中自由，则
> $$
> \dfrac{t_1 = t'_1 \quad \cdots \quad t_n = t'_n \quad \varphi(t_1, \cdots, t_n)}{\varphi(t'_1, \cdots, t'_n)} \ \text{RE}_4
> $$

易见将这两者加入一阶逻辑是等价的。

#### 例子 3.2.6

在只有 $=$ 这一个谓词符号（不含函数、其他谓词和常量）的语言中：

- (1) $\exists x_1 \exists x_2 \ \neg (x_1 = x_2)$ 表达论域中 **至少有两个** 元素。
- (2) $\forall x_1 \forall x_2 \ (x_1 = x_2)$ 表达论域中 **至多有一个** 元素。

### 3.3 理论与 Peano 算术

#### 定义 3.3.1 (理论, 公理集, 定理集)

称 $T \subset \text{CLOSE}$ 为一个理论，若 $T \vdash \varphi \Rightarrow \varphi \in T$。

任给谓词公式集 $A \subset \text{CLOSE}$（称为公理集），令 $\text{Th}(A) = \{\varphi \in \text{CLOSE} \mid A \vdash \varphi\}$，则易见 $\text{Th}(A)$ 为一个理论，称为 $A$ 的定理集。

若只允许使用语言 $L$ 中的符号作推导，则记作 $\Gamma \vdash_L \varphi$；此时若 $\Gamma \not\vdash_L \bot$，就称 $\Gamma$ 在 $L$ 中一致。相应地记 $\text{Th}_L(A) = \{\varphi \in \text{CLOSE}_L \mid A \vdash_L \varphi\}$。

#### 定义 3.3.2 (Peano 算术 / Peano Arithmetic, PA)

称结构 $\mathcal{M}$ 为 PA 的模型，若其含有一元函数 $S$、二元函数 $+, \cdot$（_下面写作习见的中缀表达式形式_）和常量 $\bar{0}$，且满足：

- (1) $\mathcal{M} \models \forall x \ \neg (S(x) = 0)$。
- (2) $\mathcal{M} \models \forall x \ \forall y \ ((S(x) = S(y)) \to (x = y))$。
- (3) $\mathcal{M} \models \forall x \ ((x + 0) = x)$。
- (4) $\mathcal{M} \models \forall x \ \forall y \ ((x + S(y)) = S(x + y))$。
- (5) $\mathcal{M} \models \forall x \ ((x \cdot 0) = 0)$。
- (6) $\mathcal{M} \models \forall x \ \forall y \ ((x \cdot S(y)) = ((x \cdot y) + x))$。
- (7) **归纳公理模式**：任给谓词公式 $\varphi$，有 $\mathcal{M} \models ((\varphi(0) \land \forall x \ (\varphi(x) \to \varphi(S(x)))) \to \forall x \ \varphi(x))$。

> _Rmk._ 在 (7) 中我们没有限制 $\varphi$ 的自由变量集合包含于 $\{x\}$ 中，因此严格地说，我们应当将其写成对自由变量显式作全称量化的形式，如此得到的才是符合 **定义 3.3.1** 的公理集。这里简洁起见没有这样记叙。

通常所说的 Peano 算术 $\text{PA}$ 指上述公理模式构成的公理集，而 $\langle \mathbb{N}; +, \cdot, S; =; \bar{0} \rangle$ 正是其 **标准模型**。

> _Rmk._ PA 既有可数模型（如上面给出的标准模型），也有不可数模型。\
> _Rmk._ 实数理论（即全部在 $\langle \mathbb{R}; +, \cdot, <, 0, 1 \rangle$ 中为真的闭式集）有可数模型，这是因为这些闭式自身是可数的；尽管我们知道它不是 $\mathbb{R}$ 本身，因为实数不可数。

### 3.4 可靠性与一致性

#### 引理 3.4.1 (替换引理)

> 设 $x$ 为变量，$t \in \text{TERM}'_{\times}$，$\varphi \in \text{CLOSE}'$，若 $t$ 对 $x$ 在 $\varphi$ 中自由，则 $\llbracket \varphi [t / x] \rrbracket_{\mathcal{M}} = \llbracket \varphi [\llbracket t \rrbracket_{\mathcal{M}} / x] \rrbracket_{\mathcal{M}}$。

_Proof._ 对 $\varphi$ 的结构归纳。项的情形由项的结构归纳即得，原子公式与联结词的情形再由 **定义 3.1.6** 即得。

下面考虑量词情形，设 $\varphi = \forall y \ \psi$：

- (i) 若 $y = x$，则两边同为 $\llbracket \forall x \ \psi \rrbracket_{\mathcal{M}}$。
- (ii) 若 $y \neq x$，则由 **规则 3.2.1** 可知 $x \not\in \text{FV}(\psi)$ 或 $y \not\in \text{FV}(t)$，两种情形下 $\displaystyle\min_{v \in \Omega}$ 都与归纳假设交换，故等式成立。

综上明所欲证。

#### 定理 3.4.2 (可靠性)

> 设 $\Gamma \subset \text{FOR}$，$\varphi \in \text{FOR}$，若 $\Gamma \vdash \varphi$，则 $\Gamma \models \varphi$。

_Proof._ 整体思路与 **定理 2.2.5** 别无二致，无非是归纳时要将 $\varphi$ 泛化到扩充常量符号后的谓词公式集 $\text{FOR}'$ 之上。

记 $(x_i)_{i \in I} = \text{FV}(\Gamma) \cup \text{FV}(\varphi)$，对 $\Gamma \vdash \varphi$ 的推导归纳，下面补充量词规则和相等公理的情形。

- (i) 若由 $\forall I$ 给出，设 $\varphi = \forall x \ \psi$，则 $\Gamma \vdash \psi$ 且 $x \not\in \text{FV}(\Gamma)$。任给 $\mathcal{M}$ 与 $(\bar{v}_i)_{i \in I} \in \Omega^I$，设 $\mathcal{M} \models \Gamma [\bar{v}_i / x_i]_{i \in I}$。
- - I. 若 $x \not\in \text{FV}(\psi)$，此时 $\text{FV}(\psi) \subset \text{FV}(\varphi)$，归纳假设给出 $\mathcal{M} \models \psi [\bar{v}_i / x_i]_{i \in I}$，于是

$$
\begin{aligned}
& \llbracket \forall x \ \psi [\bar{v}_i / x_i]_{i \in I} \rrbracket_{\mathcal{M}} \\
= \ & \min_{v \in \Omega} \llbracket (\psi [\bar{v}_i / x_i]_{i \in I}) [\bar{v} / x] \rrbracket_{\mathcal{M}} = \min_{v \in \Omega} \llbracket \psi [\bar{v}_i / x_i]_{i \in I} \rrbracket_{\mathcal{M}} = 1
\end{aligned}
$$

- - II. 若 $x \in \text{FV}(\psi)$，记 $I = I' \cup \{i_0\}$ 且 $x_{i_0} = x$。任给 $(\bar{v}_i)_{i \in I'} \in \Omega^{I'}$ 与 $\bar{v} \in \Omega$，把 $\bar{v}_{i_0}$ 取作 $\bar{v}$；由 $x \not\in \text{FV}(\Gamma)$ 有 $\mathcal{M} \models \Gamma [\bar{v}_i / x_i]_{i \in I}$，故归纳假设给出 $\mathcal{M} \models \psi [\bar{v}_i / x_i]_{i \in I}$，于是

$$
\llbracket \forall x \ \psi [\bar{v}_i / x_i]_{i \in I'} \rrbracket_{\mathcal{M}} = \min_{v \in \Omega} \llbracket (\psi [\bar{v}_i / x_i]_{i \in I'}) [\bar{v} / x] \rrbracket_{\mathcal{M}} = \min_{v \in \Omega} \llbracket \psi [\bar{v}_i / x_i]_{i \in I} \rrbracket_{\mathcal{M}} = 1
$$

- 总之 $\mathcal{M} \models \forall x \ \psi [\bar{v}_i / x_i]_{i \in I}$，故 $\Gamma \models \forall x \ \psi$。
- (ii) 若由 $\forall E$ 给出，设 $\varphi = \psi [t / x]$，$\Gamma \vdash \forall x \ \psi$ 且 $\text{Free}(\psi, x, t)$。
- - I. 若 $x \not\in \text{FV}(\psi)$，则 $\psi = \psi [t / x]$，由归纳假设即得。
- - II. 若 $x \in \text{FV}(\psi)$，记 $T = t [\bar{v}_i / x_i]_{i \in I}$，它是 $\text{TERM}'_{\times}$ 中的项。由 $x \not\in \text{FV}(\Gamma)$ 有 $\mathcal{M} \models \Gamma [\bar{v}_i / x_i]_{i \in I} = \Gamma [\bar{v}_i / x_i]_{i \in I \cup \{x\}}$，故归纳假设给出

$$
\min_{v \in \Omega} \llbracket \psi [\bar{v}_i / x_i]_{i \in I \cup \{x\}} [\bar{v} / x] \rrbracket_{\mathcal{M}} = 1
$$

- - 取 $\bar{v} = \llbracket T \rrbracket_{\mathcal{M}}$ 并由 **引理 3.4.1** 即得

$$
\llbracket \psi [t / x] [\bar{v}_i / x_i]_{i \in I} \rrbracket_{\mathcal{M}} = \llbracket \psi [\bar{v}_i / x_i]_{i \in I} [\llbracket T \rrbracket_{\mathcal{M}} / x] \rrbracket_{\mathcal{M}} = 1
$$

- 总之 $\mathcal{M} \models \psi [t / x] [\bar{v}_i / x_i]_{i \in I}$，故 $\Gamma \models \psi [t / x]$。
- (iii) 若由 $\text{RE}_i \ (i \in \{1, 2, 3, 4\})$ 给出，分类讨论：
- - I. $i \in \{1, 2, 3\}$ 的情形无非是 $\Omega$ 上相等关系的自反、对称与传递性。
- - II. $i = 4$ 的情形：$\text{RE}_4$ 的两条分别断言相等项的像相等、相等元组上谓词的真值相同，两条都由 $\llbracket t \rrbracket$ 只依赖项的值即得。
- (iv) 若由 $\exists E$ 给出，设 $\Gamma \vdash \exists x \ \psi$ 且 $\Gamma, \psi [y / x] \vdash \varphi$，其中 $y \not\in \text{FV}(\Gamma) \cup \text{FV}(\varphi)$ 且 $\text{Free}(\psi, x, y)$。
- 记 $\{x_i\}_{i \in I} = \text{FV}(\Gamma) \cup \text{FV}(\exists x \ \psi) \cup \text{FV}(\varphi)$，任给 $\mathcal{M}$ 和 $(\bar{v}_i)_{i \in I} \in \Omega^I$，设 $\mathcal{M} \models \Gamma [\bar{v}_i / x_i]_{i \in I}$。
- 由 $\Gamma \vdash \exists x \ \psi$ 的归纳假设可知 $\mathcal{M} \models \exists x \ \psi [\bar{v}_i / x_i]_{i \in I}$，即存在 $\bar{v} \in \Omega$ 使得 $\mathcal{M} \models \psi [\bar{v} / x] [\bar{v}_i / x_i]_{i \in I}$，再由 $\text{Free}(\psi, x, y)$ 可知 $\mathcal{M} \models \psi [y / x] [\bar{v} / y] [\bar{v}_i / x_i]_{i \in I}$。
- 由 $y \not\in \text{FV}(\Gamma)$ 可知 $\mathcal{M} \models \Gamma [\bar{v} / y] [\bar{v}_i / x_i]_{i \in I}$，再由 $\Gamma, \psi [y / x] \vdash \varphi$ 的归纳假设可知 $\mathcal{M} \models \varphi [\bar{v} / y] [\bar{v}_i / x_i]_{i \in I}$，而 $y \not\in \text{FV}(\varphi)$，于是 $\mathcal{M} \models \varphi [\bar{v}_i / x_i]_{i \in I}$，故 $\Gamma \models \varphi$。

综上明所欲证。

#### 定义 3.4.3 (一致, 极大一致集)

称 $\Gamma \subset \text{CLOSE}$ 一致，若 $\Gamma \not\vdash \bot$，记作 $\text{Con}(\Gamma)$；否则称 $\Gamma$ 不一致，记作 $\text{Inc}(\Gamma)$。

称一致的 $\Gamma' \subset \text{CLOSE}$ 为极大一致集，若不存在真包含 $\Gamma'$ 的一致集。

#### 命题 3.4.4 (一致性的基本性质)

> 设 $\Gamma \subset \text{CLOSE}$，$\varphi \in \text{CLOSE}$：
> > (1) 有限性：$\Gamma \vdash \varphi$ 当且仅当存在有限集 $\Gamma_0 \subset \Gamma$，使得 $\Gamma_0 \vdash \varphi$。\
> > (2) 爆炸原理：若 $\text{Inc}(\Gamma)$，则任给 $\varphi$ 有 $\Gamma \vdash \varphi$。\
> > (3) 有模型蕴涵一致：若存在模型 $\mathcal{M}$ 使得 $\mathcal{M} \models \Gamma$，则 $\text{Con}(\Gamma)$。\
> > (4) 语法紧致性：$\text{Con}(\Gamma)$ 当且仅当 $\Gamma$ 的每个有限子集一致。\
> > (5) 加入否定：$\Gamma \vdash \varphi$ 当且仅当 $\text{Inc}(\Gamma \cup \{\neg \varphi\})$。\
> > (6) 双向扩张：若 $\text{Con}(\Gamma)$，则 $\Gamma \cup \{\varphi\}$ 与 $\Gamma \cup \{\neg \varphi\}$ 至少有一个一致。

_Proof._ (1) 证明所用到的假设是有限的。

(2) 由 $\bot E$。

(3) 由 **定理 3.4.2** 可知 $\Gamma \vdash \bot$ 给出 $\Gamma \models \bot$，但 $\llbracket \bot \rrbracket_{\mathcal{M}} = 0$，矛盾！

(4) 若 $\Gamma \vdash \bot$，由 (1) 可知存在有限集 $\Gamma_0 \subset \Gamma$，使得 $\Gamma_0 \vdash \bot$；反之由 $\Gamma_0 \subset \Gamma$ 和单调性可知。

(5) 若 $\Gamma \vdash \varphi$，则 $\Gamma, \neg \varphi \vdash \bot$；反之若 $\Gamma, \neg \varphi \vdash \bot$，由 $\text{RAA}$ 有 $\Gamma \vdash \varphi$。

(6) 若两者皆不一致，由 (5) 可知 $\Gamma \vdash \varphi$、由 $\text{RAA}$ 可知 $\Gamma \vdash \neg \varphi$，故 $\Gamma \vdash \bot$，与 $\text{Con}(\Gamma)$ 矛盾！

#### 命题 3.4.5

> 设 $L_0 \subset L_1 \subset \cdots$ 为递增的语言序列，$\Gamma_0 \subset \Gamma_1 \subset \cdots$ 为递增的闭式集序列，若每个 $\Gamma_n$ 都在 $L_n$ 中一致，则 $\displaystyle\bigcup_{n \in \mathbb{N}} \Gamma_n$ 在 $\displaystyle\bigcup_{n \in \mathbb{N}} L_n$ 中一致。

_Proof._ 若不然，$\displaystyle\bigcup_{n \in \mathbb{N}} \Gamma_n \vdash_{\bigcup_{n \in \mathbb{N}} L_n} \bot$，由 **命题 3.4.4 (1)** 可知其推导只涉及有限多个前提，则全部落在同一个 $\Gamma_N$ 中；同时推导中出现的符号也只有有限多个，故取足够大的 $M$，$\Gamma_M$ 在 $L_M$ 中已不一致，矛盾！

### 3.5 完备性定理

#### 定义 3.5.1 (Henkin 理论, 否定完备)

相较可靠性而言，完备性的证明颇显迂回。

设 $T$ 是语言 $L$ 上的一个理论：

- (1) 称 $T$ 为 Henkin 理论，若任给闭式 $\exists x \ \varphi(x) \in T$，都存在常量 $\bar{c}$，使得 $\varphi(\bar{c}) \in T$，此时称 $\bar{c}$ 为 $\exists x \ \varphi(x)$ 的 **见证 (witness)**。
- (2) 称 $T$ 否定完备，若任给闭式 $\theta$，$\theta \in T$ 与 $\neg \theta \in T$ 至少有一个成立。

#### 命题 3.5.2

> 设理论 $T$ 一致、否定完备且是 Henkin 理论，则任给闭式 $\theta, \chi$：
> > (1) $T \vdash \theta$ 当且仅当 $T \not\vdash \neg \theta$。\
> > (2) $T \vdash \theta \lor \chi$ 当且仅当 $T \vdash \theta$ 或 $T \vdash \chi$。\
> > (3) $T \vdash \exists x \ \theta$ 当且仅当存在常量 $\bar{c}$ 使得 $T \vdash \theta [\bar{c} / x]$。

_Proof._ (1) 若 $T \vdash \theta$，由一致性可知 $T \not\vdash \neg \theta$；反之若 $T \not\vdash \neg \theta$，由否定完备性可知 $T \vdash \theta$。

(2) 若 $T \vdash \theta$ 或 $T \vdash \chi$，由 $\lor I$ 可知 $T \vdash \theta \lor \chi$；反之若 $T \not\vdash \theta$ 且 $T \not\vdash \chi$，由 (1) 有 $T \vdash \neg \theta$ 且 $T \vdash \neg \chi$，与 $T \vdash \theta \lor \chi$ 一并由 $\lor E$ 导出 $\bot$，矛盾！

(3) 若 $T \vdash \theta [\bar{c} / x]$，由 $\exists I$ 可知 $T \vdash \exists x \ \theta$；反之由 $T \vdash \exists x \ \theta$ 与 $T$ 对 $\vdash$ 封闭可知 $\exists x \ \theta \in T$，再由 $T$ 为 Henkin 理论可取见证 $\bar{c}$ 即得 $T \vdash \theta [\bar{c} / x]$。

#### 引理 3.5.3 (Henkin 扩张)

> 任给语言 $L$ 上一致的理论 $T$，都存在语言 $L \subset L'$ 与 $L'$ 上一致的理论 $T^* \supset T$，使得：
> > (1) $T^*$ 是 Henkin 理论；\
> > (2) $T^*$ 是 $T$ 的 **保守扩张**，即若 $\theta \in \text{CLOSE}_L$ 且 $T^* \vdash \theta$，则 $T \vdash \theta$。

_Proof._ 考虑归纳扩张。先令 $L_0 = L, \Phi_0 = T$。

接下来设 $n \in \mathbb{N}$，对每个 $\text{CLOSE}_{L_n}$ 中的存在公式 $\exists x \ \psi$，取一个该层互不相同且不在 $L_n$ 中的新常量 $\bar{c}_{\exists x \ \psi}$，并令

$$
\begin{cases}
L_{n + 1} = L_n \cup \{\bar{c}_{\exists x \ \psi} \mid \exists x \ \psi \in \text{CLOSE}_{L_n}\} \\
\Phi_{n + 1} = \Phi_n \cup \{(\exists x \ \psi \to \psi [\bar{c}_{\exists x \ \psi} / x]) \mid \exists x \ \psi \in \text{CLOSE}_{L_n}\}
\end{cases}
$$

最后令 $L' = \displaystyle\bigcup_{n \in \mathbb{N}} L_n, \Psi = \bigcup_{n \in \mathbb{N}} \Phi_n$，以及 $T^* = \text{Th}_{L'}(\Psi)$。

先证 $\forall n \in \mathbb{N}$，$\Phi_n$ 在 $L_n$ 中一致。考虑归纳，假设 $\Phi_n$ 在 $L_n$ 中一致但 $\Phi_{n + 1}$ 在 $L_{n + 1}$ 中不一致，即 $\Phi_{n + 1} \vdash_{L_{n + 1}} \bot$。

矛盾的推导只应使用有限条新增公理，设最后出现的一条为 $A_c = (\exists x \ \psi \to \psi [\bar{c} / x])$，其余有限多条合并记为 $\Gamma$，则由 $\Gamma, A_c \vdash \bot$ 得 $\Gamma \vdash \exists x \ \psi$ 且 $\Gamma, \psi [\bar{c} / x] \vdash \bot$。

常量 $\bar{c}$ 不会出现在 $\Gamma$ 与 $\exists x \ \psi$ 中；把推导里的 $\bar{c}$ 换为未出现过的新变量 $y$，此时 $y \not\in \text{FV}(\Gamma) \cup \text{FV}(\bot)$（$\Gamma$ 中都是闭式），故由 $\exists E$ 可知 $\Gamma, \exists x \ \psi \vdash \bot$，与 $\Gamma \vdash \exists x \ \psi$ 合并即 $\Gamma \vdash \bot$：重复消去其余用到的新增公理，便得 $\Phi_n \vdash_{L_n} \bot$，矛盾！

总之由 **命题 3.4.5** 可知 $\Psi$ 在 $L'$ 中一致，从而 $T^* = \text{Th}_{L'}(\Psi)$ 一致且 $T^* \supset T$。

(1) 任给闭式 $\exists x \ \psi \in \text{CLOSE}_{L'}$，它只含有限多个符号，故属于某个 $\text{CLOSE}_{L_N}$，其见证公理已在 $\Phi_{N + 1} \subset \Psi \subset T^*$ 中；若 $\exists x \ \psi \in T^*$，由 $\to E$ 即得 $\psi [\bar{c} / x] \in T^*$，故 $T^*$ 是 Henkin 理论。

(2) 若 $\theta \in \text{CLOSE}_L$ 且 $T^* \vdash \theta$，则 $\Psi \vdash_{L'} \theta$，其证明只用到 $\Phi_n$ 中有限多条公理，故只用到某个 $\Phi_N$；把这些新常量在推导中一致改名为未出现过的新变量，即得 $\Phi_N \vdash_{L_N} \theta$。

下面逐层下降：设 $\Gamma \subset \Phi_{n + 1}$ 有限、$\theta \in \text{CLOSE}_{L_n}$ 且 $\Gamma \vdash_{L_{n + 1}} \theta$。若 $\Gamma$ 中的公理都已在 $\Phi_n$ 中则已，否则取其中最后出现的新公理 $A_c = (\exists x \ \psi \to \psi [\bar{c} / x])$，对重言式 $\exists x \ \psi \lor \neg \exists x \ \psi$ 用 $\lor E$ 分类讨论：

- - I. 若 $\Gamma \vdash \neg \exists x \ \psi$，则 $A_c$ 的前件为假，于是 $\Gamma \vdash A_c$，故 $\Gamma, A_c \vdash \theta$ 给出 $\Gamma \vdash \theta$，故 $A_c$ 可删去。
- - II. 若 $\Gamma \vdash \exists x \ \psi$，则由 $\Gamma, A_c \vdash \theta$ 与 $A_c$ 的定义可知 $\Gamma, \psi [\bar{c} / x] \vdash \theta$，把其中的 $\bar{c}$ 换为未出现过的新变量 $y$ 得 $\Gamma, \psi [y / x] \vdash \theta$，再由 $\exists E$ 得 $\Gamma \vdash \theta$（$y \not\in \text{FV}(\Gamma) \cup \text{FV}(\theta)$，$\Gamma$ 与 $\theta$ 都是闭式），故 $A_c$ 同样可删去。

如此逐条删完即得 $\Phi_n \vdash_{L_n} \theta$；再对 $n = N, N - 1, \cdots, 0$ 累加，最终得到 $T \vdash \theta$。

#### 引理 3.5.4

> 设 $L'$ 是可数语言，$\Psi \subset \text{CLOSE}_{L'}$ 一致，则存在极大一致的 $\Omega \supset \Psi$；进一步，极大一致集否定完备。

_Proof._ 将全部闭式枚举为 $\text{CLOSE}_{L'} = \{\theta_n\}_{n \in \mathbb{N}}$，归纳构造一致集的升链：

$$
\Psi_0 = \Psi, \quad \Psi_{n + 1} = \begin{cases}
\Psi_n \cup \{\theta_n\}, &\quad \Psi_n \cup \{\theta_n\} \text{ 一致} \\
\Psi_n, &\quad \text{otherwise}
\end{cases}
$$

再令 $\Omega = \displaystyle\bigcup_{n \in \mathbb{N}} \Psi_n$。

先证 $\Omega$ 一致。若 $\Omega \vdash \bot$，由 **命题 3.4.4 (1)** 其推导只使用有限多个前提，它们全落在某个 $\Psi_N \subset \Omega$ 中，于是 $\Psi_N \vdash \bot$，与 $\Psi_N$ 一致矛盾！

再证 $\Omega$ 极大。若不然，设一致集 $\Delta \supsetneq \Omega$，由于枚举穷尽了所有闭式，必有 $n \in \mathbb{N}$ 使得 $\theta_n \in \Delta \backslash \Omega$，由构造可知 $\Psi_n \cup \{\theta_n\}$ 不一致，而 $\Psi_n \cup \{\theta_n\} \subset \Delta$，故 $\Delta$ 也不一致，矛盾！

最后证 $\Omega$ 否定完备。任给闭式 $\theta$，由 **命题 3.4.4 (6)** 可知 $\Omega \cup \{\theta\}$ 与 $\Omega \cup \{\neg \theta\}$ 至少有一个一致。故极大性给出 $\Omega \cup \{\theta\} = \Omega$ 或 $\Omega \cup \{\neg \theta\} = \Omega$，这就是说 $\theta \in \Omega$ 或 $\neg \theta \in \Omega$，故 $\Omega \vdash \theta$ 或 $\Omega \vdash \neg \theta$。

#### 定义 3.5.5 (项模型)

设 $\Omega$ 是 $L'$ 上一致的理论，记 $\text{T}_{L'}$ 为其全体闭项。在 $\text{T}_{L'}$ 上定义

$$
t_1 \sim t_2 \Leftrightarrow \Omega \vdash t_1 = t_2
$$

由 **规则 3.2.5** 中 $\text{RE}_1, \text{RE}_2, \text{RE}_3$ 可知 $\sim$ 为等价关系。以等价类 $\bar{t}$ 为元素定义项模型 $\mathcal{T}_{\Omega}$：

- (1) 论域为 $\text{T}_\Omega = \text{T}_{L'} / \sim$。
- (2) $f^{\mathcal{T}_{\Omega}}(\bar{t}_1, \cdots, \bar{t}_n) = \overline{f(t_1, \cdots, t_n)}$。
- (3) $P^{\mathcal{T}_{\Omega}} = \{(\bar{t}_1, \cdots, \bar{t}_n) \mid \Omega \vdash P(t_1, \cdots, t_n)\}$，相等符号则按论域上的相等解释。
- (4) $c^{\mathcal{T}_{\Omega}} = \bar{c}$。

由 **规则 3.2.5** 中 $\text{RE}_4$ 的两条可知 (2)(3) 的定义与代表元的选取无关；对项的结构归纳即得 $\llbracket t \rrbracket_{\mathcal{T}_{\Omega}} = \bar{t}$。

#### 引理 3.5.6 (Henkin 真值引理)

> 设 $\Omega$ 是 $L'$ 上一致、否定完备的 Henkin 理论，则任给闭式 $\theta$，$\Omega \vdash \theta$ 当且仅当 $\mathcal{T}_{\Omega} \models \theta$。

_Proof._ 对 $\theta$ 的结构归纳，只需考虑 $\bot, \neg, \lor, \exists$ 四种情形（这里我们将 $\forall$ 反过来视作 $\exists$ 的衍生物）：

- (i) $\theta = P(t_1, \cdots, t_n)$ 或 $\theta = t_1 = t_2$：由 **定义 3.5.5** 与 $\llbracket t \rrbracket_{\mathcal{T}_{\Omega}} = \bar{t}$ 立即可得。
- (ii) $\theta = \bot$：由 $\Omega$ 一致可知 $\Omega \not\vdash \bot$，同时 $\llbracket \bot \rrbracket_{\mathcal{T}_{\Omega}} = 0$，命题成立。
- (iii) $\theta = \neg \psi$：
- - $\Rightarrow$：若 $\Omega \vdash \neg \psi$，由 **命题 3.5.2 (1)** 可知 $\Omega \not\vdash \psi$，由归纳假设可知 $\mathcal{T}_{\Omega} \not\models \psi$，故 $\mathcal{T}_{\Omega} \models \neg \psi$。
- - $\Leftarrow$：若 $\mathcal{T}_{\Omega} \models \neg \psi$，则 $\mathcal{T}_{\Omega} \not\models \psi$，由归纳假设可知 $\Omega \not\vdash \psi$，由 **命题 3.5.2 (1)** 可知 $\Omega \vdash \neg \psi$。
- (iv) $\theta = \psi_1 \lor \psi_2$：
- - $\Rightarrow$：若 $\Omega \vdash \psi_1 \lor \psi_2$，由 **命题 3.5.2 (2)** 可知 $\Omega \vdash \psi_1$ 或 $\Omega \vdash \psi_2$，由归纳假设可知 $\mathcal{T}_{\Omega} \models \psi_1$ 或 $\mathcal{T}_{\Omega} \models \psi_2$，故 $\mathcal{T}_{\Omega} \models \psi_1 \lor \psi_2$。
- - $\Leftarrow$：若 $\mathcal{T}_{\Omega} \models \psi_1 \lor \psi_2$，则 $\mathcal{T}_{\Omega} \models \psi_1$ 或 $\mathcal{T}_{\Omega} \models \psi_2$，由归纳假设可知 $\Omega \vdash \psi_1$ 或 $\Omega \vdash \psi_2$，由 **命题 3.5.2 (2)** 可知 $\Omega \vdash \psi_1 \lor \psi_2$。
- (v) $\theta = \exists x \ \psi$：
- - $\Rightarrow$：若 $\Omega \vdash \exists x \ \psi$，由 **命题 3.5.2 (3)** 可知存在常量 $\bar{c}$ 使得 $\Omega \vdash \psi [\bar{c} / x]$，由归纳假设可知 $\mathcal{T}_{\Omega} \models \psi [\bar{c} / x]$，故 $\mathcal{T}_{\Omega} \models \exists x \ \psi$。
- - $\Leftarrow$：若 $\mathcal{T}_{\Omega} \models \exists x \ \psi$，由于论域是闭项的等价类，存在闭项 $t$ 使得 $\mathcal{T}_{\Omega} \models \psi [t / x]$（把语义见证经 **引理 3.4.1** 换成该项），由归纳假设可知 $\Omega \vdash \psi [t / x]$，由 $\exists I$ 可知 $\Omega \vdash \exists x \ \psi$。

综上明所欲证。

#### 引理 3.5.7 (重合引理)

> 设结构 $\mathcal{M}, \mathcal{M}'$ 有相同的论域，且在 $\varphi$ 中出现的非逻辑符号上解释相同，则 $\mathcal{M} \models \varphi$ 当且仅当 $\mathcal{M}' \models \varphi$。

_Proof._ 对 $\varphi$ 的结构归纳即可。

#### 定理 3.5.8 (模型存在引理)

> 设 $L$ 为可数语言，$\Phi \subset \text{CLOSE}_L$ 一致，则存在结构 $\mathcal{M}$ 使得 $\mathcal{M} \models \Phi$。

_Proof._ 取 $T = \text{Th}_L(\Phi)$，由 **引理 3.5.3** 得语言 $L' \supset L$ 与 $L'$ 上一致的 Henkin 理论 $T^* \supset T$，则 $L'$ 仍为可数语言。再由 **引理 3.5.4** 取极大一致且否定完备的 $\Omega \supset T^*$。

由于 $T^* \subset \Omega$，且 $T^*$ 中包含所有见证公理，而 $\Omega$ 对 $\vdash$ 封闭，故当 $\exists x \ \varphi(x) \in \Omega$，由 $\to E$ 可知 $\varphi(\bar{c}) \in \Omega$。故由 **引理 3.5.6** 可知其项模型满足

$$
\Omega \vdash \theta \Leftrightarrow \mathcal{T}_{\Omega} \models \theta
$$

任给 $\theta \in \Phi \subset T \subset T^* \subset \Omega$，有 $\Omega \vdash \theta$；故 $\mathcal{T}_{\Omega} \models \Phi$。

最后把 $\mathcal{T}_{\Omega}$ 限制回原语言 $L$：由 **引理 3.5.7**，$L$ 上的公式的真值只依赖 $L$ 中符号的解释，故所得结构仍是 $\Phi$ 的模型。明所欲证。

#### 定理 3.5.9 (完备性)

> 设 $L$ 为可数语言，$\Gamma \subset \text{CLOSE}_L$，$\varphi \in \text{CLOSE}_L$，若 $\Gamma \models \varphi$，则 $\Gamma \vdash \varphi$。

_Proof._ 反设 $\Gamma \not\vdash \varphi$，由 **命题 3.4.4 (5)** 知 $\Gamma \cup \{\neg \varphi\}$ 一致，由 **定理 3.5.8** 它有模型 $\mathcal{M}$，于是 $\mathcal{M} \models \Gamma$ 而 $\mathcal{M} \not\models \varphi$，与 $\Gamma \models \varphi$ 矛盾！

故 $\Gamma \vdash \varphi$，明所欲证。
