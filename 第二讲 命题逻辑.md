### 2.1 基本概念

#### 定义 2.1.1 (字母表)

定义下列符号构成命题逻辑的字母表 $\Sigma$：

- (1) 一族事先确定的原子命题 $\text{At}$（如 $P, Q, P_1, P_2$）和假命题 $\bot$。
- (2) 连接词，包括 $\neg, \land, \lor, \to$。
- (3) 括号，包括 $(, )$。

一般要求 $\text{At}$ 至多可数。

#### 定义 2.1.2 (命题公式)

称 $\Sigma$ 中元素组成的 **有限长** 序列构成句子集合

$$
\Sigma^* = \bigsqcup_{n \in \mathbb{N}} \Sigma^n
$$

但并非所有这些句子都“有意义”；下面取命题公式集合 $\text{FOR} \subset \Sigma^*$ 为满足下列条件的 **最小** 集合：

- (1) 所有原子命题都在 $\text{FOR}$ 中。
- (2) 若 $\varphi \in \text{FOR}$，则 $\neg \varphi \in \text{FOR}$。
- (3) 若 $\varphi, \psi \in \text{FOR}$，则 $(\varphi \land \psi), (\varphi \lor \psi), (\varphi \to \psi) \in \text{FOR}$。

> _Rmk._ 当然为便书写，人类自然会在不致歧义时扔掉多余的括号。

#### 引理 2.1.3 (命题公式的结构归纳法)

> 任给 $\Sigma^*$ 上的谓词 $A$，若：
> > (1) 任给原子命题 $P$，$A(P)$ 为真，且 $A(\bot)$ 为真；\
> > (2) 若 $A(\varphi)$ 为真，则 $A(\neg \varphi)$ 为真；\
> > (3) 若 $A(\varphi), A(\psi)$ 为真，则 $A(\varphi \land \psi), A(\varphi \lor \psi), A(\varphi \to \psi)$ 为真。
>
> 则 $\forall \varphi \in \text{FOR}$，$A(\varphi)$ 为真。

_Proof._ 设 $S = \{\varphi \in \Sigma^* \mid A(\varphi)\}$，则由条件可见 $S$ 满足上面 $\text{FOR}$ 的三个条件，由 $\text{FOR}$ 的最小性即得 $\text{FOR} \subset S$，明所欲证。

#### 定义 2.1.4 (命题公式的赋值 / 语义)

称 $V : \text{FOR} \to \{0, 1\}$ 为 $\text{FOR}$ 的一个赋值，若：

- (1) $V(\bot) = 0$；
- (2) $V(\neg \varphi) = 1 - V(\varphi)$；
- (3) $V(\varphi \land \psi) = \min(V(\varphi), V(\psi))$；
- (4) $V(\varphi \lor \psi) = \max(V(\varphi), V(\psi))$；
- (5) $V(\varphi \to \psi) = \max(1 - V(\varphi), V(\psi))$。

也记 $\varphi \in \text{FOR}$ 在 $V$ 下的赋值为 $\llbracket \varphi \rrbracket_V$。

> _Rmk._ 可以说 $V$ 由所有映射 $V_{\text{At}} : \text{At} \to \{0, 1\}$ 经上述规则延拓而来。

#### 定义 2.1.5 (重言式, 语义蕴涵式)

称 $\varphi \in \text{FOR}$ 为重言式，若任给赋值 $V$，都有 $\llbracket \varphi \rrbracket_V = 1$，记作 $\models \varphi$。

进一步地，设 $\Gamma$ 代表一族命题（“上下文”）：

- (1) 定义 $V \models \Gamma$ 为 $\forall \varphi \in \Gamma, \llbracket \varphi \rrbracket_V = 1$。
- (2) 定义 $\Gamma \models \varphi$ 为任给满足 $V \models \Gamma$ 的赋值 $V$，都有 $\llbracket \varphi \rrbracket_V = 1$。

### 2.2 自然演绎法

#### 规则 2.2.1 (自然演绎法则)

> _Rmk._ 下面的自然演绎系统只以 $\land, \to$ 为连接词。下面出现的 $\neg \varphi$ 应当理解为 $\varphi \to \bot$ 的简记。

> (1) 合取的消去法则
> $$
> \frac{\varphi \land \psi}{\varphi} \land E, \quad \frac{\varphi \land \psi}{\psi} \land E
> $$
> (2) 合取的引入法则
> $$
> \frac{\varphi \quad \psi}{\varphi \land \psi} \land I
> $$
> (3) 蕴涵的消去法则
> $$
> \frac{\varphi \quad \varphi \to \psi}{\psi} \to E
> $$
> (4) 蕴涵的引入法则
> $$
> \frac{\dfrac{\dfrac{[\varphi]}{\cdots}}{\psi}}{\varphi \to \psi} \to I
> $$
> 上面的部分即“假设 $\varphi$ 成立时可以推导出 $\psi$”。\
> (5) 反证法 (RAA, _Reductio ad Absurdum_)
> $$
> \frac{\neg \varphi \to \bot}{\varphi} \ \text{RAA}
> $$
> 或者等价地写成：
> $$
> \frac{\dfrac{\dfrac{[\neg \varphi]}{\cdots}}{\bot}}{\varphi} \ \text{RAA}
> $$
> (6) 矛盾的消去法则 / 爆炸原理
> $$
> \frac{\bot}{\varphi} \ \bot \ E
> $$

若以 $\Gamma$ 中的命题为前提可推知 $\varphi$，则记作 $\Gamma \vdash \varphi$。

按照上述规则可将推导过程画成一棵树，这将在下面的例子中展示。

#### 例子 2.2.2

下面在自然演绎框架中证明 $P \vdash (P \to Q) \to Q$。

$$
\frac{\dfrac{P \quad [P \to Q]}{Q} \to E}{(P \to Q) \to Q} \to I
$$

#### 例子 2.2.3

下面在自然演绎框架中证明 $\neg \neg P \vdash P$。

$$
\frac{\dfrac{\neg P \to \bot \quad [\neg P]}{\bot} \to E}{P} \ \text{RAA}
$$

#### 例子 2.2.4

下面在自然演绎框架中 **不用 $\text{RAA}$** 证明 $\neg \neg \neg P \vdash \neg P$。

$$
\frac{\dfrac{\dfrac{\dfrac{[P]_1 \quad [P \to \bot]_2}{\bot} \to E}{(P \to \bot) \to \bot} \to I_2 \quad ((P \to \bot) \to \bot) \to \bot}{\bot} \to E}{P \to \bot} \to I_1
$$

> _Rmk._ 这里标注 $I_1, I_2, [P]_1, [P \to \bot]_2$ 是为了区分两个假设分别由哪个蕴涵式所引入。

可以发现当证明链条变长，推导树将不便观察。方便起见，一种处理是将其写成缩进列表：

$$
\begin{array}{l}
& [P] \\
& \vdots & [P \to \bot] \\
& \vdots & \cdots & \bot &\quad \to E \\
& \vdots & (P \to \bot) \to \bot & &\quad \to I \\
& \vdots & \bot & &\quad \to E \\
& P \to \bot & & &\quad \to I
\end{array}
$$

其中每个 $[\varphi]$ 和对应的 $\varphi \to \psi$ 间用缩进标识假设 $\varphi$ 生效范围。

另一种处理是将每个命题公式、生成其规则和其所用的假设集合制作为表格：

| 编号 | 公式 | 规则 | 假设集合 |
| --- | --- | --- | --- |
| $1$ | $((P \to \bot) \to \bot) \to \bot$ | 前提 | |
| $2$ | $P$ | 引入假设 $①$ | $①$ |
| $3$ | $P \to \bot$ | 引入假设 $②$ | $②$ |
| $4$ | $\bot$ | $\to E_{2, 3}$ | $①, ②$ |
| $5$ | $(P \to \bot) \to \bot$ | $\to I_{3, 4}$ | $①$ |
| $6$ | $\bot$ | $\to E_{5, 1}$ | $①$ |
| $7$ | $P \to \bot$ | $\to I_{2, 6}$ | |

------

接下来我们将说明自然演绎法“为什么正确”，具体来说分为两个方面：

- (1) **一致性 (Soundness)**：凡是能用自然演绎法推出的命题都是正确（符合语义）的。
- (2) **完备性 (Completeness)**：凡是正确的命题都能用自然演绎法推出。

#### 定理 2.2.5 (一致性)

> 设 $\varphi \in \text{FOR}$，若 $\Gamma \vdash \varphi$，则 $\Gamma \models \varphi$。

_Proof._ 对 $\Gamma \vdash \varphi$ 的推导归纳：

- (i) 若由 $\land E$ 给出，则 $\Gamma \vdash \varphi \land \psi$ 或 $\Gamma \vdash \psi \land \varphi$（不妨设为前者），由归纳假设可知 $\Gamma \models \varphi \land \psi$。
- 这就是说任给赋值 $V$，只要 $V \models \Gamma$，就有 $\llbracket \varphi \land \psi \rrbracket_V = \min(\llbracket \varphi \rrbracket_V, \llbracket \psi \rrbracket_V) = 1$，推出 $\llbracket \varphi \rrbracket_V = 1$，故 $\Gamma \models \varphi$。
- (ii) 若由 $\land I$ 给出，设 $\varphi = \psi_1 \land \psi_2$，则 $\Gamma \vdash \psi_1$ 且 $\Gamma \vdash \psi_2$，由归纳假设可知 $\Gamma \models \psi_1$ 且 $\Gamma \models \psi_2$。
- 这就是说任给赋值 $V$，只要 $V \models \Gamma$，就有 $\llbracket \psi_1 \rrbracket_V = 1$ 且 $\llbracket \psi_2 \rrbracket_V = 1$，推出 $\llbracket \psi_1 \land \psi_2 \rrbracket_V = \min(\llbracket \psi_1 \rrbracket_V, \llbracket \psi_2 \rrbracket_V) = 1$，故 $\Gamma \models \psi_1 \land \psi_2$。
- (iii) 若由 $\to E$ 给出，则 $\Gamma \vdash \psi$ 且 $\Gamma \vdash \psi \to \varphi$，由归纳假设可知 $\Gamma \models \psi$ 且 $\Gamma \models \psi \to \varphi$。
- 这就是说任给赋值 $V$，只要 $V \models \Gamma$，就有 $\llbracket \psi \rrbracket_V = 1$，且 $\llbracket \psi \rrbracket_V = 1$ 蕴涵 $\llbracket \varphi \rrbracket_V = 1$，推出 $\llbracket \varphi \rrbracket_V = 1$，故 $\Gamma \models \varphi$。
- (iv) 若由 $\to I$ 给出，设 $\varphi = \psi_1 \to \psi_2$，则 $\Gamma, \psi_1 \vdash \psi_2$，由归纳假设可知 $\Gamma, \psi_1 \models \psi_2$。
- 这就是说任给赋值 $V$，只要 $V \models \Gamma, \psi_1$，就有 $\llbracket \psi_2 \rrbracket_V = 1$；这无非是说只要 $V \models \Gamma$，就有 $\llbracket \psi_1 \rrbracket_V = 1$ 蕴涵 $\llbracket \psi_2 \rrbracket_V = 1$，故 $\Gamma \models \psi_1 \to \psi_2$。
- (v) 若由 RAA 给出，则 $\Gamma, \varphi \to \bot \vdash \bot$，由归纳假设可知 $\Gamma, \varphi \to \bot \models \bot$。
- 这就是说任给赋值 $V$，只要 $V \models \Gamma, \varphi \to \bot$，就有 $\llbracket \bot \rrbracket_V = 1$，然而这与 $\llbracket \bot \rrbracket_V = 0$ 矛盾：这无非是说只要 $V \models \Gamma$，就有 $\llbracket \varphi \to \bot \rrbracket_V = 1 - \llbracket \varphi \rrbracket_V = 1$ 蕴涵矛盾，因此此时只能有 $\llbracket \varphi \rrbracket_V = 1$，故 $\Gamma \models \varphi$。
- (vi) 若由 $\bot E$ 给出，则 $\Gamma \vdash \bot$，由归纳假设可知 $\Gamma \models \bot$。
- 这就是说任给赋值 $V$，只要 $V \models \Gamma$，就有 $\llbracket \bot \rrbracket_V = 1$，然而这与 $\llbracket \bot \rrbracket_V = 0$ 矛盾；故空真给出 $\Gamma \models \varphi$。

综上明所欲证：一言以蔽之，这无非是定义的操演。

#### 定理 2.2.6 (完备性)

> 设 $\varphi \in \text{FOR}$，若 $\Gamma \models \varphi$，则 $\Gamma \vdash \varphi$。

相较一致性而言，完备性的证明颇显迂回。

#### 定义 2.2.7 (一致, 极大一致集)

称 $\Gamma \subset \text{FOR}$ 一致，若 $\Gamma \not\vdash \bot$。

称一致的 $\Gamma' \subset \text{FOR}$ 为极大一致集，若不存在真包含 $\Gamma'$ 的一致集。

#### 引理 2.2.8 (Lindenbaum)

> 设 $\Gamma$ 一致，则存在极大一致集 $\Gamma' \supset \Gamma$。

_Proof._ 将全部命题公式枚举为 $\text{FOR} = \{\varphi_n\}_{n \in \mathbb{N}}$，归纳构造 **一致集** 的升链：

$$
\Gamma_0 = \Gamma, \quad \Gamma_{n + 1} = \begin{cases}
\Gamma_n \cup \{\varphi_n\}, & \Gamma_n \cup \{\varphi_n\} \text{ 一致} \\
\Gamma_n, & \text{otherwise}
\end{cases}
$$

再令 $\Gamma' = \displaystyle\bigcup_{n \in \mathbb{N}} \Gamma_n$。

先证 $\Gamma'$ 一致。若 $\Gamma' \vdash \bot$，其推导树只有有限个叶子，故而也只使用了 $\Gamma'$ 中的有限个前提，这些前提全部落在某个 $\Gamma_N \subset \Gamma' \ (N \in \mathbb{N})$ 中，于是 $\Gamma_N \vdash \bot$，与 $\Gamma_N$ 一致矛盾！故 $\Gamma' \not\vdash \bot$，即 $\Gamma'$ 一致。

再证 $\Gamma'$ 极大。若不然，设一致集 $\Delta \supsetneq \Gamma'$，由于构造过程穷尽了所有命题公式，必有 $n \in \mathbb{N}$ 使得 $\varphi_n \in \Delta \backslash \Gamma'$，由构造过程可知 $\Gamma_n \cup \{\varphi_n\}$ 不一致，而 $\Gamma_n \cup \{\varphi_n\} \subset \Delta$，则 $\Delta$ 亦不一致，矛盾！故 $\Gamma'$ 为极大一致集。

#### 命题 2.2.9

> 设 $\Gamma'$ 为极大一致集，则：
> > (1) 封闭性：若 $\Gamma' \vdash \varphi$，则 $\varphi \in \Gamma'$。\
> > (2) 完备性：$\forall \varphi \in \text{FOR}$，有 $\Gamma' \vdash \varphi$ 或 $\Gamma' \vdash \neg \varphi$。

_Proof._ (1) 若 $\varphi \notin \Gamma'$，一方面 $\Gamma' \cup \{\varphi\}$ 随 $\Gamma'$ 一致，另一方面其真包含 $\Gamma'$，与极大性矛盾！故 $\varphi \in \Gamma'$。

(2) 若 $\Gamma' \not\vdash \varphi$，必有 $\varphi \notin \Gamma'$，则 $\Gamma' \cup \{\varphi\} \supsetneq \Gamma'$，由极大性可知其不一致，即 $\Gamma', \varphi \vdash \bot$，由 $\to I$ 可得 $\Gamma' \vdash \varphi \to \bot$ 即 $\Gamma' \vdash \neg \varphi$。故 $\Gamma' \vdash \varphi, \Gamma' \vdash \neg \varphi$ 二者必居其一。

#### 引理 2.2.10

> 设 $\Gamma'$ 为极大一致集，定义赋值 $V$：对于 $p \in \text{At}$，令 $\llbracket p \rrbracket_V = [p \in \Gamma']$，则：
> $$
> \varphi \in \Gamma' \Leftrightarrow \llbracket \varphi \rrbracket_V = 1
> $$

_Proof._ 对 $\varphi$ 的结构归纳：

- (i) $\varphi = p \in \text{At}$：由 $V$ 的定义立即可得 $p \in \Gamma' \Leftrightarrow \llbracket p \rrbracket_V = 1$。
- (ii) $\varphi = \bot$：由 $\Gamma'$ 一致可知 $\bot \notin \Gamma'$，同时由 $V$ 的定义有 $\llbracket \bot \rrbracket_V = 0$，命题成立。
- (iii) $\varphi = \psi_1 \land \psi_2$：
- - $\Rightarrow$：若 $\psi_1 \land \psi_2 \in \Gamma'$，由 $\land E$ 及 **命题 2.2.9 (1)** 可知 $\psi_1, \psi_2 \in \Gamma'$，再由归纳假设可知 $\llbracket \psi_1 \rrbracket_V = \llbracket \psi_2 \rrbracket_V = 1$，故 $\llbracket \psi_1 \land \psi_2 \rrbracket_V = 1$。
- - $\Leftarrow$：若 $\llbracket \psi_1 \land \psi_2 \rrbracket_V = 1$，则 $\llbracket \psi_1 \rrbracket_V = \llbracket \psi_2 \rrbracket_V = 1$，再由归纳假设可知 $\psi_1, \psi_2 \in \Gamma'$，由 $\land I$ 及 **命题 2.2.9 (1)** 可知 $\psi_1 \land \psi_2 \in \Gamma'$。
- (iv) $\varphi = \psi_1 \to \psi_2$：
- - $\Rightarrow$：若 $\psi_1 \to \psi_2 \in \Gamma'$，当 $\llbracket \psi_1 \rrbracket_V = 1$，由归纳假设可知 $\psi_1 \in \Gamma'$，由 $\to E$ 及 **命题 2.2.9 (1)** 可知 $\psi_2 \in \Gamma'$，由归纳假设可知 $\llbracket \psi_2 \rrbracket_V = 1$；故必有 $\llbracket \psi_1 \to \psi_2 \rrbracket_V = \max(1 - \llbracket \psi_1 \rrbracket_V, \llbracket \psi_2 \rrbracket_V) = 1$。
- - $\Leftarrow$：若 $\llbracket \psi_1 \to \psi_2 \rrbracket_V = 1$，有 $\llbracket \psi_1 \rrbracket_V = 0$ 或 $\llbracket \psi_2 \rrbracket_V = 1$。
- - - I. 当 $\llbracket \psi_1 \rrbracket_V = 0$，由归纳假设可知 $\psi_1 \not\in \Gamma'$，由 **命题 2.2.9** 可知 $\neg \psi_1 \in \Gamma'$，于是 $\to E$ 给出 $\Gamma', \psi_1 \vdash \bot$，然后 $\bot E$ 给出 $\Gamma', \psi_1 \vdash \psi_2$，最后 $\to I$ 给出 $\Gamma' \vdash \psi_1 \to \psi_2$，由 **命题 2.2.9 (1)** 即得 $\psi_1 \to \psi_2 \in \Gamma'$。
- - - II. 当 $\llbracket \psi_2 \rrbracket_V = 1$，由归纳假设可知 $\psi_2 \in \Gamma' \subset \Gamma' \cup \{\psi_1\}$，由 $\to I$ 及 **命题 2.2.9 (1)** 可知 $\psi_1 \to \psi_2 \in \Gamma'$。

综上明所欲证。

------

至此回到 **定理 2.2.6** 的证明。

_Proof._ 当 $\Gamma \models \varphi$，假设 $\Gamma \not\vdash \varphi$，则 $\Gamma \cup \{\neg \varphi\}$ 一致：若不然，$\Gamma, \neg \varphi \vdash \bot$，由 RAA 可知 $\Gamma \vdash \varphi$，矛盾！

由 **引理 2.2.8** 可取极大一致集 $\Gamma' \supset \Gamma \cup \{\neg \varphi\}$，由 **引理 2.2.10** 取赋值 $V$ 使 $\psi \in \Gamma' \Leftrightarrow \llbracket \psi \rrbracket_V = 1$，则 $\llbracket \neg \varphi \rrbracket_V = 1$ 即 $\llbracket \varphi \rrbracket_V = 0$，但 $\Gamma \models \varphi$ 要求 $\llbracket \varphi \rrbracket_V = 1$，矛盾！

故 $\Gamma \vdash \varphi$，明所欲证。

> _Rmk._ 在不含 $\text{RAA}$ 的直觉主义命题逻辑中仍可证明：
> $$
> P \to \neg \neg P, \quad \neg \neg \neg P \to \neg P, \quad (P \to Q) \to (\neg Q \to \neg P)
> $$
> 而下列命题的证明则必须依赖 $\text{RAA}$：
> $$
> \neg \neg P \to P, \quad (\neg Q \to \neg P) \to (P \to Q), \quad ((P \to Q) \to P) \to P
> $$
> 若需证明某命题 $\varphi$ “去掉 $\text{RAA}$ 后不可证明”，基本的思想是：
> > (1) 构造一种新的语义（如 Kripke 模型），证明去掉 $\text{RAA}$ 后的自然演绎法 $\vdash_i$ 与这种语义下的 $\models_i$ 相容，即一致性和完备性成立；\
> > (2) 在这种语义下 $\models_i \varphi$ 不成立。

### 2.3 System K

#### 定义 2.3.1 (System K 的语法)

> _Rmk._ 简洁起见，这里不再像 **2.1** 开头那样严格地阐述语法。

给定原子命题集合 $\text{At}$，命题公式集合 $\text{FOR}$ 归纳定义如下：

- (1) 所有原子命题都在 $\text{FOR}$ 中。
- (2) 若 $\varphi \in \text{FOR}$，则 $\neg \varphi, \square \varphi, \diamondsuit \varphi \in \text{FOR}$，其中后面两者分别表示必然 $\varphi$ 与可能 $\varphi$。
- (3) 若 $\varphi, \psi \in \text{FOR}$，则 $(\varphi \land \psi), (\varphi \lor \psi), (\varphi \to \psi) \in \text{FOR}$。

也可写作 BNF 形式：

$$
\varphi ::= p \mid \bot \mid \neg \varphi \mid \square \varphi \mid \diamondsuit \varphi \mid (\varphi \land \varphi) \mid (\varphi \lor \varphi) \mid (\varphi \to \varphi)
$$

#### 定义 2.3.2 (相继式)

相继式指形如 $\Gamma \Rightarrow \Delta$ 的表达式，其中 $\Gamma, \Delta \subset \text{FOR}$ 为有限集。

直观地说，$\Gamma$ 是假设集，$\Delta$ 是结论集，其语义为：若 $\Gamma$ 中公式都为真，则 $\Delta$ 中至少有一式为真。

#### 规则 2.3.3 (相继式演算法则)

> _Rmk._ 这里没有涉及模态算子 $\square, \diamondsuit$ 的法则。

> (1) 初始公理：
> $$
> \frac{}{\varphi \Rightarrow \varphi} \ \text{ID}
> $$
> (2) 弱化规则：
> $$
> \frac{\Gamma \Rightarrow \Delta}{\varphi, \Gamma \Rightarrow \Delta} \ \text{Weak}_L, \quad \frac{\Gamma \Rightarrow \Delta}{\Gamma \Rightarrow \Delta, \varphi} \ \text{Weak}_R
> $$
> (3) 否定规则：
> $$
> \frac{\Gamma \Rightarrow \Delta, \varphi}{\neg \varphi, \Gamma \Rightarrow \Delta} \ \neg L, \quad \frac{\varphi, \Gamma \Rightarrow \Delta}{\Gamma \Rightarrow \Delta, \neg \varphi} \ \neg R
> $$
> (4) 合取规则：
> $$
> \frac{\varphi, \psi, \Gamma \Rightarrow \Delta}{\varphi \land \psi, \Gamma \Rightarrow \Delta} \land L, \quad \frac{\Gamma \Rightarrow \Delta, \varphi \quad \Gamma \Rightarrow \Delta, \psi}{\Gamma \Rightarrow \Delta, \varphi \land \psi} \land R
> $$
> (5) 析取规则：
> $$
> \frac{\varphi, \Gamma \Rightarrow \Delta \quad \psi, \Gamma \Rightarrow \Delta}{\varphi \lor \psi, \Gamma \Rightarrow \Delta} \lor L, \quad \frac{\Gamma \Rightarrow \Delta, \varphi}{\Gamma \Rightarrow \Delta, \varphi \lor \psi} \lor R_1, \quad \frac{\Gamma \Rightarrow \Delta, \psi}{\Gamma \Rightarrow \Delta, \varphi \lor \psi} \lor R_2
> $$
> (6) 蕴涵规则：
> $$
> \frac{\Gamma \Rightarrow \Delta, \varphi \quad \psi, \Gamma \Rightarrow \Delta}{\varphi \to \psi, \Gamma \Rightarrow \Delta} \to L
> \quad
> \frac{\varphi, \Gamma \Rightarrow \Delta, \psi}{\Gamma \Rightarrow \Delta, \varphi \to \psi} \to R
> $$
