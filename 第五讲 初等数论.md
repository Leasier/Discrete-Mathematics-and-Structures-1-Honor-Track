### 5.1 RSA

#### 算法 5.1.1 (RSA)

设 $p, q$ 为大质数，称 $N = pq$ 和整数 $e$ 构成 **公钥 (public key)**，称 $p, q$ 构成 **私钥 (private key)**，其中 $e \in \mathbb{Z}_{\varphi(N)}^{\times}$。

对于 $\mathbb{Z}_N^{\times}$ 中的消息 $m$：

- 用公钥加密：算出 $c = m^e \bmod N$。
- 用私钥解密：首先算出 $d$ 使得 $ed \equiv 1 \pmod{\varphi(N)}$，然后取得 $m = c^d \bmod N$。正确性由欧拉定理确保。

不严格地说，RSA 的安全性来自 **质因数分解** 的困难性，即仅仅已知 $N$ 很难算出 $p, q$。

### 5.2 Stern-Brocot 树

#### 定义 5.2.1 (Stern-Brocot 树)

设 $\dfrac{a}{b} < \dfrac{c}{d}$ 为两个分数，称 $\dfrac{a + c}{b + d}$ 为它们的 **中位分数**。

Stern-Brocot 树递归定义如下：

- 根为 $\dfrac{1}{1}$，其区间为 $\left( \dfrac{0}{1}, \dfrac{1}{0} \right)$，其中约定 $\dfrac{1}{0} = +\infty$。
- 设节点 $\dfrac{p}{q} = \dfrac{a + c}{b + d}$ 的区间为 $\left( \dfrac{a}{b}, \dfrac{c}{d} \right)$，则：
- - (1) 它的左子节点为 $\dfrac{a + p}{b + q}$，区间为 $\left( \dfrac{a}{b}, \dfrac{p}{q} \right)$。
- - (2) 它的右子节点为 $\dfrac{p + c}{q + d}$，区间为 $\left( \dfrac{p}{q}, \dfrac{c}{d} \right)$。

若 $\dfrac{a}{b}, \dfrac{c}{d}$ 是树中某个节点区间的两端，则称它们是**相邻**的。称从根到某节点所经过的方向序列（$L$ 表示走左子节点、$R$ 表示走右子节点）为该节点的**路径**，其长度为该节点的**深度**，根的深度为 $0$。

#### 命题 5.2.2 (严格有序性)

> 设 $\dfrac{a}{b} < \dfrac{c}{d} \ (b, d > 0)$，则 $\dfrac{a}{b} < \dfrac{a + c}{b + d} < \dfrac{c}{d}$。

_Proof._ 由 $\dfrac{a}{b} < \dfrac{c}{d}$ 与 $b, d > 0$ 可知 $bc - ad > 0$。

由 $b(a + c) - a(b + d) = bc - ad > 0$ 可知 $\dfrac{a}{b} < \dfrac{a + c}{b + d}$；同理由 $(b + d)c - (a + c)d = bc - ad > 0$ 可知 $\dfrac{a + c}{b + d} < \dfrac{c}{d}$。

故 $\dfrac{a}{b} < \dfrac{a + c}{b + d} < \dfrac{c}{d}$，明所欲证。

#### 命题 5.2.3

> 设 $\dfrac{a}{b}, \dfrac{c}{d}$ 是树中相邻的一对分数，则 $bc - ad = 1$。

_Rmk._ 一个形象的说法是两个分数的“叉积”为 $1$。当然我们将在后面看到这与线性代数的关系。

_Proof._ 对树的构造过程归纳：

- (1) 初始相邻的一对是 $\dfrac{0}{1}, \dfrac{1}{0}$，此时 $bc - ad = 1 \cdot 1 - 0 \cdot 0 = 1$。
- (2) 设相邻的一对 $\dfrac{a}{b} < \dfrac{c}{d}$ 满足 $bc - ad = 1$，在它们之间插入中位分数 $\dfrac{a + c}{b + d}$，则 $b(a + c) - a(b + d) = (b + d)c - (a + c)d = bc - ad = 1$。

综上树中相邻的一对分数 $\dfrac{a}{b}, \dfrac{c}{d}$ 皆满足 $bc - ad = 1$，明所欲证。

#### 命题 5.2.4 (最简性)

> 树中每个节点都是最简分数。

_Proof._ 设节点 $\dfrac{p}{q}$ 的区间为 $\left( \dfrac{a}{b}, \dfrac{c}{d} \right)$，则 $\dfrac{p}{q} = \dfrac{a + c}{b + d}$，且由 **命题 5.2.3** 可知 $bc - ad = 1$。

设 $g \in \mathbb{N}_+$ 满足 $g \mid a + c$ 且 $g \mid b + d$，则 $g \mid (b + d)c - (a + c)d = bc - ad = 1$，故只能有 $g = 1$。

因此 $\gcd(p, q) = \gcd(a + c, b + d) = 1 \Rightarrow p \perp q$，即 $\dfrac{p}{q}$ 是最简分数。

#### 定理 5.2.5 (与正有理数一一对应)

> 每个正有理数在树中恰好出现一次。

_Proof._ 先证存在性。对 $m + n$ 归纳，证明每个 $\dfrac{m}{n} \ (m, n \in \mathbb{N}_+)$ 都出现在树中：

- (1) 若 $m = n$，则 $\dfrac{m}{n} = \dfrac{1}{1}$ 是根。
- (2) 若 $m < n$，则 $\dfrac{m}{n} = \dfrac{a}{a + b}$ 当且仅当 $\dfrac{a}{b} = \dfrac{m}{n - m}$，而 $m + (n - m) = n < m + n$，由归纳假设可知 $\dfrac{m}{n - m}$ 出现在树中，故 $\dfrac{m}{n}$ 出现在左子树中。
- (3) 若 $m > n$，则 $\dfrac{m}{n} = \dfrac{a + b}{b}$ 当且仅当 $\dfrac{a}{b} = \dfrac{m - n}{n}$，而 $(m - n) + n = m < m + n$，由归纳假设可知 $\dfrac{m - n}{n}$ 出现在树中，故 $\dfrac{m}{n}$ 出现在右子树中。

再证唯一性。设节点 $u$ 的区间为 $(L_u, R_u)$（若 $R_u = \dfrac{1}{0}$ 则理解为 $+\infty$），由 **命题 5.2.2** 可知 $L_u < u < R_u$。

- (1) 若 $u, v$ 中有一个是另一个的真祖先，则后代的值落在祖先的某个子区间内，而该子区间以祖先的值为端点，故 $u \neq v$。
- (2) 若 $u, v$ 互不为祖先，设 $w$ 是它们在树中的最近公共祖先，则 $u, v$ 分别落在 $w$ 的两个子区间 $(L_w, w)$ 与 $(w, R_w)$ 内，而这两个区间不交，故 $u \neq v$。

故不同节点的值互不相同。

综上，每个正有理数在树中恰好出现一次，明所欲证。

#### 例子 5.2.6

树上深度为 $0, 1, 2$ 的节点依次为：

$$
\frac{1}{1}
$$

$$
\frac{1}{2} \quad \frac{2}{1}
$$

$$
\frac{1}{3} \quad \frac{2}{3} \quad \frac{3}{2} \quad \frac{3}{1}
$$

这些节点连同两个边界按大小排列，得到

$$
\frac{0}{1} < \frac{1}{3} < \frac{1}{2} < \frac{2}{3} < \frac{1}{1} < \frac{3}{2} < \frac{2}{1} < \frac{3}{1} < \frac{1}{0}
$$

其中每个分数都最简，且在这一行中相邻两项的叉积都是 $1$。

#### 算法 5.2.7 (在树中查找分数)

给定正有理数 $r = \dfrac{m}{n} \ (m, n \in \mathbb{N}_+)$，欲求其在树中的位置。

从区间 $\left( \dfrac{0}{1}, \dfrac{1}{0} \right)$ 出发，反复把 $r$ 与当前区间 $\left( \dfrac{a}{b}, \dfrac{c}{d} \right)$ 的中位分数 $\dfrac{a + c}{b + d}$ 比较：

- (1) 若 $r = \dfrac{a + c}{b + d}$，则找到 $r$，过程结束。
- (2) 若 $r < \dfrac{a + c}{b + d}$，则以 $\left( \dfrac{a}{b}, \dfrac{a + c}{b + d} \right)$ 继续。
- (3) 若 $r > \dfrac{a + c}{b + d}$，则以 $\left( \dfrac{a + c}{b + d}, \dfrac{c}{d} \right)$ 继续。

由 **定理 5.2.5** 可知过程必在有限步内结束，且每走一步对应路径上的一个 $L$ 或 $R$。

不过直接暴力走的时间复杂度是 $O(\max(m, n))$ 的，因为 $L^{n - 1}$ 对应于 $\dfrac{1}{n}$。

一个自然的优化是连续同向的若干步可以一次走完。设 $r \in \left( \dfrac{a}{b}, \dfrac{c}{d} \right)$，则向右连走 $k$ 步后的区间为 $\left( \dfrac{a + kc}{b + kd}, \dfrac{c}{d} \right)$、向左连走 $k$ 步后的区间为 $\left( \dfrac{a}{b}, \dfrac{ka + c}{kb + d} \right)$。

令 $u = mb - na, v = nc - md$，由

$$
r > \frac{a + kc}{b + kd} \Leftrightarrow k < \frac{u}{v}, \quad r < \frac{ka + c}{kb + d} \Leftrightarrow k < \frac{v}{u}
$$

可知向右最多连走 $\left\lceil \dfrac{u}{v} \right\rceil - 1$ 步、向左最多连走 $\left\lceil \dfrac{v}{u} \right\rceil - 1$ 步。

若这一步向右走，要么 $v \mid u$：过程在这一段结束；否则 $u$ 将变为 $u \bmod v$。向左的情形是同理的。

容易发现这无非是欧几里得算法的操演，故时间复杂度为 $O(\log \min(m, n))$。

#### 定义 5.2.8 (LR 串, Stern-Brocot 数系)

记 $\Sigma = \{L, R\}$，对字符串 $S \in \Sigma^*$：

- (1) 称 $|S|$ 为 $S$ 的长度；若 $S$ 是树中某个节点 $v$ 的路径，则 $|S|$ 就是 $v$ 的深度。
- (2) 记 $f(S)$ 为路径恰是 $S$ 的那个节点的值，特别地 $f(\varepsilon) = \dfrac{1}{1}$。
- (3) 若 $S$ 是 $v$ 的路径，则 $SL$ 与 $SR$ 分别是 $v$ 的左、右子节点的路径。
- (4) 由 (2)(3) 与 **定理 5.2.5** 可知 $f : \Sigma^* \to \mathbb{Q}_+$ 是双射，其中 $f$ 把每个路径映到树中相应的正有理数。

故也称 $\Sigma^*$ 为 **Stern-Brocot 数系**，它是正有理数的一种表示方式。

#### 定义 5.2.9 (区间矩阵)

设节点 $v$ 的区间为 $\left( \dfrac{a}{b}, \dfrac{c}{d} \right)$，称

$$
M = \begin{pmatrix}
b & d \\
a & c
\end{pmatrix}
$$

为 $v$ 的区间矩阵，即两列分别由区间端点的分母和分子组成。记

$$
M_0 = \begin{pmatrix}
1 & 0 \\
0 & 1
\end{pmatrix}, \quad L = \begin{pmatrix}
1 & 1 \\
0 & 1
\end{pmatrix}, \quad R = \begin{pmatrix}
1 & 0 \\
1 & 1
\end{pmatrix}
$$

则 $M_0$ 是根节点的区间矩阵，而容易验证向左、右子树走一步恰好对应于区间矩阵右乘 $L, R$，同时易见 $|L| = |R| = 1$。

#### 命题 5.2.10 (区间矩阵的行列式)

> 树上任一节点的区间矩阵的行列式为 $1$。

_Rmk._ 这无非是 **命题 5.2.3** 的线性代数表述。

_Proof._ 对节点的深度归纳：

- (1) 根节点的区间矩阵 $M_0$ 的行列式显然为 $1$。
- (2) 设节点 $v$ 的区间矩阵为 $M$，由归纳假设可知 $|M| = 1$，故其左、右儿子的区间矩阵 $ML, MR$ 的行列式为 $|M| \cdot |L| = |M| \cdot |R| = |M| = 1$。

综上，树上任一节点的区间矩阵的行列式为 $1$，明所欲证。
